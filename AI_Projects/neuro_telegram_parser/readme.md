# Telegram Parser: Интеллектуальный агрегатор и аналитический ассистент новостных потоков Telegram

Приложение для автоматического парсинга, анализа и классификации новостных потоков из Telegram с использованием современных технологий машинного обучения и обработки естественного языка.

## 🎯 Основные возможности

- **📡 Парсинг Telegram-каналов** - автоматический сбор сообщений из множества каналов
- **🧠 Семантический поиск** - поиск по смыслу через векторные эмбеддинги (FRIDA)
- **📊 Тематическое моделирование** - автоматическое выявление тем и событий (BERTopic)
- **🏷️ Классификация сообщений** - отнесение к предустановленным темам (categorize_topic)
- **📈 Анализ трендов** - обнаружение всплесков интереса через статистический анализ
- **🔄 Дедупликация событий** - объединение похожих новостей в единые события
- **📝 Генерация заголовков** - автоматическое создание заголовков через OpenAI GPT
- **🔐 Система аутентификации** - JWT токены, роли пользователей, защита API

-----

## Шаг 1: Авторизация в Telegram (`python -m telegram_parser.setup_main_session`)

Этот шаг является **критически важным и обязательным**. Без его выполнения приложение не сможет подключиться к Telegram.

  - **Процесс**: Запустите команду пакета `python -m telegram_parser.setup_main_session` в вашем терминале.
  - **Взаимодействие**: Скрипт запросит ваш номер телефона, код подтверждения, который придет в Telegram, и, возможно, пароль двухфакторной аутентификации.
  - **Результат**: После успешной авторизации в корневой директории проекта будет создан файл **`telegram_parser.session`**. Этот файл содержит ключ аутентификации и позволяет приложению в дальнейшем подключаться без повторного ввода данных.

*Механизм обработки сессий в проекте достаточно продвинут. Скрипт `setup_main_session.py` создает мастер-файл сессии **`telegram_parser.session`**. Затем, когда фоновый обработчик запускает задачу, `TelegramClientManager` (`telegram_client_manager.py`) использует эту мастер-сессию для экспорта временной строки сессии. Новый клиент для задачи создается в памяти на основе этой строки. Этот подход позволяет избежать конфликтов доступа к файлу сессии, если несколько обработчиков работают одновременно, и избавляет от необходимости повторной авторизации. Именно поэтому первоначальная настройка так важна.*

-----

## Шаг 2: Инициализация основной базы данных (`python -m telegram_parser.init_db`)

  - **Назначение**: Этот скрипт создает необходимые таблицы (`channels`, `messages`) и индексы в базе данных PostgreSQL, указанной в переменной `POSTGRES_DSN`.
  - **Команда**: Выполните команду `python -m telegram_parser.init_db`.
  - **Обзор схемы**: Скрипт создаст таблицу `channels` для хранения метаданных (ID, имя, описание) и таблицу `messages` для хранения самих сообщений с привязкой к каналу.

-----

## Шаг 3: Инициализация системы аутентификации

  - **Назначение**: Создание таблицы пользователей и дефолтного администратора для системы аутентификации.
  - **Команда**: Выполните миграцию `python migrations/004_auth_users.py` и инициализацию `python auth/init_admin.py`.
  - **Автоматическая инициализация**: При запуске приложения (`python app.py`) дефолтный администратор создается автоматически, если таблица `users` пуста.
  - **Первый вход**: 
    - Логин: `admin`
    - Пароль: значение из переменной окружения `DEFAULT_ADMIN_PASSWORD` (по умолчанию `admin123`)
    - ⚠️ **ВАЖНО**: Измените пароль после первого входа!

### Эндпоинты аутентификации:

| Метод | Эндпоинт | Описание |
| :--- | :--- | :--- |
| **POST** | `/auth/login` | Вход пользователя (возвращает JWT токен) |
| **GET** | `/auth/me` | Получить информацию о текущем пользователе (требует токен) |
| **POST** | `/auth/verify` | Проверить валидность токена |
| **POST** | `/auth/users` | Создать нового пользователя (только для администраторов) |
| **GET** | `/auth/users` | Получить список всех пользователей (только для администраторов) |
| **POST/GET** | `/auth/logout` | Выход из системы |

### Создание пользователей:

#### Способ 1: Через API (требуется токен администратора)

```bash
# 1. Войдите как администратор и получите токен
curl -X POST http://localhost:5000/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"qwerty"}'

# 2. Создайте нового пользователя
curl -X POST http://localhost:5000/auth/users \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <ваш_токен>" \
  -d '{
    "username": "analyst1",
    "password": "secure_password123",
    "email": "analyst@example.com",
    "role": "analyst"
  }'
```

#### Способ 2: Через командную строку (скрипт)

```bash
# Создать аналитика
python scripts/create_user.py --username analyst1 --password secure123

# Создать администратора
python scripts/create_user.py --username admin2 --password admin456 --role admin

# Создать пользователя с email
python scripts/create_user.py --username user1 --password pass123 --email user@example.com --role analyst
```

#### Способ 3: Через Python код

```python
import asyncio
from auth.user_service import user_service

async def create_user():
    user = await user_service.create_user(
        username="newuser",
        password="secure_password",
        email="user@example.com",
        role="analyst"  # или "admin"
    )
    if user:
        print(f"Пользователь создан: {user['username']}")

asyncio.run(create_user())
```

### Защита роутов:

Используйте декораторы из `auth.dependencies`:

```python
from auth.dependencies import require_auth, require_role

@app.route('/api/protected')
@require_auth
def protected_route(current_user):
    return jsonify({"user": current_user})

@app.route('/api/admin/clusters')
@require_role('admin')
def admin_route(current_user):
    return jsonify({"message": "Admin only"})
```

-----

# Запуск приложения

Для полноценной работы системы необходимо запустить два отдельных процесса в разных терминалах.
### Быстрый старт (Windows PowerShell)

```powershell
# 1) Установка зависимостей
python -m pip install -r requirements.txt

# 2) Создание .env (отредактируйте значения под себя)
@"
TELEGRAM_API_ID=1
TELEGRAM_API_HASH=456789abcdef01234
TELEGRAM_PHONE_NUMBER=+70000000000
POSTGRES_DSN=postgresql://user:password@localhost:5432/telegram_parser_db
POSTGRES_CUSTOMER_DSN=postgresql://user:password@localhost:5432/telegram_data_customer
"@ | Out-File -Encoding UTF8 .env

# 3) Инициализация БД
python -m telegram_parser.init_db

# 4) Авторизация Telegram (создание telegram_parser.session)
python -m telegram_parser.setup_main_session

# 5) Запуск воркера Huey (в отдельном окне)
python -m telegram_parser.huey_consumer

# 6) Запуск веб-сервера (в отдельном окне)
python -m telegram_parser.app
```

## 🚀 Pro-режим: Интеллектуальный агрегатор и аналитический ассистент новостных потоков Telegram

После базовой настройки вы можете активировать **Pro-режим** - продвинутую систему семантического поиска, тематического моделирования, классификации и аналитики новостных потоков.

### Возможности Pro-режима:

- **🧠 Семантический поиск** - поиск по смыслу через векторные эмбеддинги (FRIDA модель)
- **📊 Тематическое моделирование** - автоматическое выявление тем и событий через BERTopic
- **🏷️ Классификация сообщений** - отнесение сообщений к предустановленным темам через `categorize_topic`
- **🔄 Дедупликация событий** - объединение похожих новостей в единые события (кластеры)
- **📈 Анализ трендов** - обнаружение всплесков интереса через статистический анализ (z-score)
- **📝 Генерация заголовков** - автоматическое создание заголовков для событий через OpenAI GPT
- **📋 Персонализированная лента** - фильтрация и ранжирование сообщений по интересам
- **📊 Детальная аналитика** - метрики по темам, событиям, каналам

### Технологический стек Pro-режима:

**Модели машинного обучения:**
- **FRIDA (ai-forever/FRIDA)** - многоязычная модель для семантического поиска и классификации (размерность 1536)
  - Режим `search_document` - для индексации документов
  - Режим `search_query` - для кодирования поисковых запросов
  - Режим `categorize_topic` - для классификации по темам
- **GTE (Alibaba-NLP/gte-multilingual-base)** - модель для кластеризации (размерность 768)
- **BERTopic** - библиотека для тематического моделирования с UMAP и HDBSCAN
- **OpenAI GPT (gpt-3.5-turbo)** - для генерации заголовков событий

**Инфраструктура:**
- **Qdrant** - векторная база данных для хранения эмбеддингов (2 коллекции: `posts_search`, `posts_clustering`)
- **PostgreSQL** - реляционная база данных для хранения структурированных данных
- **Redis** - очередь задач и кеширование

### Установка Pro-режима:

**Требования:**
- Qdrant (векторная база данных) - запускается отдельно или через Docker
- OpenAI API ключ (опционально, для генерации заголовков)
- Python пакеты из `requirements.txt` (устанавливаются автоматически)

**Зависимости:**
- `sentence-transformers>=2.2.0` - для FRIDA и GTE
- `bertopic>=0.15.0` - для тематического моделирования
- `umap-learn>=0.5.3` - для снижения размерности
- `qdrant-client>=1.6.0` - для работы с Qdrant
- `openai>=1.0.0` - для генерации заголовков (опционально)

### Использование Pro-режима:

1. Запустите основное приложение и войдите через систему аутентификации
2. Перейдите на дашборд Pro-режима (`/pro/`)
3. Настройте тематическое моделирование:
   - Выберите модели и параметры кластеризации
   - Настройте классификацию (порог уверенности, выбор тем)
   - Запустите пайплайн тематического моделирования
4. Используйте функции:
   - **Семантический поиск** - поиск сообщений по смыслу
   - **Тренды** - просмотр актуальных событий с обнаружением всплесков
   - **Лента** - персонализированный поток сообщений
   - **Отчеты** - автоматически формируемые отчеты в `artifacts/topic_modeling/`

### Архитектура Pro-режима:

```
pro_mode/
├── __init__.py                    # Основной сервис координации
├── api.py                         # Flask API endpoints для Pro-режима
├── embedding_service.py           # Работа с эмбеддингами (FRIDA, GTE, Qdrant)
├── topic_modeling_service.py      # Тематическое моделирование (BERTopic)
├── deduplication_service.py       # Дедупликация и создание событий
├── classification_service.py       # Классификация сообщений по темам
├── openai_title_generator.py      # Генерация заголовков через OpenAI GPT
└── tasks_pro.py                   # Фоновые задачи Huey для Pro-режима
```

**База данных (PostgreSQL):**
- `topics` - предустановленные темы для классификации
- `message_topics` - связи сообщений с темами (результаты классификации)
- `dedup_clusters` - события/кластеры (результаты тематического моделирования)
- `cluster_messages` - связи сообщений с кластерами
- `embeddings` - метаданные об эмбеддингах (хранятся в Qdrant)

**Векторная база данных (Qdrant):**
- `posts_search` - коллекция для семантического поиска (FRIDA, размерность 1536)
- `posts_clustering` - коллекция для кластеризации (GTE, размерность 768)

**Отчеты:**
- `artifacts/topic_modeling/topic_modeling.json` - подробный отчет о результатах тематического моделирования и классификации


## Процесс 1: Запуск фонового обработчика Huey

**Команда:**

```bash
python -m telegram_parser.huey_consumer
```

  - **Ожидаемый вывод**: В терминале появятся сообщения, информирующие о том, что обработчик запущен и ожидает задач, например: `Huey Consumer запущен`.
  - **Поведение**: Этот терминал будет отображать логи выполнения задач в реальном времени.

## Процесс 2: Запуск веб-сервера Flask

**Команда:**

```bash
python -m telegram_parser.app
```

  - **Ожидаемый вывод**: В терминале появится сообщение о запуске Flask-сервера с указанием URL-адреса, по которому доступно приложение (например, `Running on http://127.0.0.1:5000/`).
  - **Доступ**: Откройте указанный URL в вашем веб-браузере, чтобы получить доступ к панели управления парсером.

-----

## ⚙️ Настройка переменных окружения (`.env`)

Создайте файл с именем **`.env`** в корневой директории проекта. Этот файл предназначен для хранения конфиденциальных данных и не должен попадать в систему контроля версий. Функция `load_dotenv()` автоматически загружает переменные из этого файла.

#### Шаблон файла `.env`:

См. файл `.env.example` для полного списка переменных окружения.

**Обязательные переменные для аутентификации:**
```
SECRET_KEY=your_strong_secret_key_change_in_production_min_32_chars
DEFAULT_ADMIN_PASSWORD=change_me_strong_password
ACCESS_TOKEN_EXPIRE_MINUTES=60
```

*В проекте предусмотрена работа с двумя базами данных: основной для парсера и дополнительной для утилит обработки данных. Файл `config.ini` ссылается на `ENV:POSTGRES_DSN` для основного приложения и `ENV:POSTGRES_CUSTOMER_DSN` для конфигурации "customer". Скрипт `init_db_customer.py` специально ищет секцию `[postgresql_customer]`. Это означает, что для использования всех возможностей проекта может потребоваться настройка двух отдельных баз данных.*

#### Описание переменных окружения

| Переменная | Назначение | Пример |
| :--- | :--- | :--- |
| **TELEGRAM\_API\_ID** | Ваш персональный API ID приложения Telegram. | `1234567` |
| **TELEGRAM\_API\_HASH** | Ваш персональный API hash приложения Telegram. | `0123456789abcdef0123456789abcdef` |
| **TELEGRAM\_PHONE\_NUMBER** | Номер телефона, привязанный к вашему аккаунту Telegram, используется для первоначальной авторизации. | `+79123456789` |
| **POSTGRES\_DSN** | Полная строка подключения (DSN) к основной базе данных PostgreSQL, где будут храниться спарсенные данные. | `postgresql://user:password@localhost:5432/telegram_parser_db` |
| **POSTGRES\_CUSTOMER\_DSN** | DSN для второй, "клиентской" базы данных, используемой скриптами офлайн-обработки данных из папки `DB_process/`. | `postgresql://user:password@localhost:5432/telegram_data_customer` |

###  Проверка конфигурации приложения (`config.ini`)

Файл **`config.ini`** содержит несекретные настройки приложения. Убедитесь, что они соответствуют вашей среде.

  - **`[telegram]`**: Связывает учетные данные с переменными из файла `.env`.
  - **`[redis]`**: Параметры подключения к серверу Redis.
  - **`[postgresql]`**: Связывает DSN основной базы данных с переменной из `.env`.
  - **`[application]`**: Определяет `channel_source_directory` — папку, в которой приложение ищет CSV-файлы с каналами для парсинга.

-----

## 🏗️ Архитектура системы

Для понимания принципов работы telegram\_parser важно рассмотреть взаимодействие его ключевых компонентов. Система имеет разделенную (декаплированную) архитектуру, что обеспечивает ее гибкость, масштабируемость и отказоустойчивость.

#### Архитектурная диаграмма (концептуальная)

Поток данных и команд в системе можно представить следующей схемой:

```
Пользователь -> Веб-интерфейс / REST API (Flask) -> Redis (Очередь задач) -> Huey Worker -> Telegram API (Pyrogram) -> PostgreSQL
```

### Компоненты системы

1.  **Веб-сервер (Flask - `app.py`)**:

      - **Роль**: Основной компонент, с которым взаимодействует пользователь. Он обслуживает фронтенд и предоставляет REST API.
      - **Функциональность**: Принимает HTTP-запросы, проверяет данные и ставит задачи парсинга в очередь. Сам веб-сервер **не выполняет парсинг**, а лишь делегирует его, обеспечивая мгновенную реакцию интерфейса.

2.  **Очередь задач и менеджер состояний (Redis)**:

      - **Роль**: Центральная нервная система приложения.
      - **Функциональность**: Выполняет две ключевые функции:
        1.  Служит брокером сообщений для Huey, храня задачи.
        2.  Хранит в реальном времени статус и прогресс каждой задачи, которые Flask отображает в панели мониторинга.

3.  **Фоновый обработчик (Huey - `huey_consumer.py`)**:

      - **Роль**: "Рабочая лошадка" системы. Это отдельный, постоянно работающий процесс.
      - **Функциональность**: Слушает очередь задач в Redis. Как только появляется новая задача, он забирает ее и выполняет код из `tasks.py`, который осуществляет парсинг.

4.  **Клиент Telegram API (Pyrogram - `parser_app/`)**:

      - **Роль**: Мост для взаимодействия с платформой Telegram.
      - **Функциональность**: `TelegramClientManager` отвечает за аутентификацию. `ChannelParser` и `ChannelSearcher` используют этот клиент для получения данных.

5.  **База данных (PostgreSQL)**:

      - **Роль**: Слой постоянного хранения данных.
      - **Функциональность**: `PostgresWriter` (`data_writer.py`) используется фоновым обработчиком для записи обработанных данных в базу.

*Разделение веб-сервера (`app.py`) и фонового обработчика (`huey_consumer.py`) является ключевым архитектурным решением. Оно позволяет веб-серверу оставаться отзывчивым даже при выполнении множества ресурсоемких задач парсинга.*

-----

## 📋 Системные требования

  - **Python**: Рекомендуется версия 3.10 или выше (протестировано на 3.11).
  - **Redis Server**: Работающий экземпляр Redis (для очереди задач Huey).
  - **PostgreSQL Server**: Работающий экземпляр PostgreSQL (рекомендуется версия 12 или выше).
  - **Qdrant**: Векторная база данных для Pro-режима (можно запустить через Docker или отдельно).
  - **Учетные данные Telegram API**: Действительные `api_id` и `api_hash` с сайта my.telegram.org.
  - **OpenAI API ключ** (опционально): Для генерации заголовков событий через GPT.

**Рекомендуемые ресурсы для Pro-режима:**
  - **CPU**: 8+ ядер
  - **RAM**: 16+ GB (для загрузки моделей FRIDA и GTE)
  - **Диск**: 10+ GB (для моделей и данных)
  - **GPU** (опционально): NVIDIA GPU для ускорения обработки эмбеддингов

-----

## 💻 Использование приложения
### Пошаговая инструкция запуска с нуля (Windows)

1) Установите Python 3.10+ и добавьте в PATH.
2) Установите Redis и PostgreSQL; создайте БД, указанную в DSN.
3) Установите зависимости:

```powershell
python -m pip install -r requirements.txt
```

4) Создайте `.env` и выполните инициализацию БД и авторизацию Telegram одной командой:

```powershell
./static/setup_env_and_db.ps1 -ApiId "<YOUR_API_ID>" -ApiHash "<YOUR_API_HASH>" -Phone "+7XXXXXXXXXX" -PostgresDsn "postgresql://user:pass@localhost:5432/telegram_parser_db"
```

5) В отдельном окне запустите воркера Huey:

```powershell
./static/run_worker.ps1
```

6) Запустите веб‑сервер:

```powershell
./static/run_web.ps1
```

7) Откройте браузер: `http://127.0.0.1:5000/` и используйте UI.


### Часть А: Веб-интерфейс

Панель управления, доступная в браузере, предоставляет полный контроль над процессом парсинга.

1.  **Парсинг из файла**:

      - Выберите CSV-файл из выпадающего списка. Информационный блок покажет количество каналов в нем.
      - Укажите желаемое количество каналов и лимит сообщений.
      - Нажмите кнопку **"Запустить парсинг из файла"**.

2.  **Ad-Hoc парсинг**:

      - Используйте поле поиска для нахождения каналов по ключевым словам.
      - В результатах поиска отметьте галочками нужные каналы.
      - Нажмите кнопку **"Запустить парсинг для выбранных"**.

3.  **Панель мониторинга**:

      - В таблице отображаются все запущенные задачи, их ID, тип, статус и динамический прогресс-бар.

### Часть Б: API эндпоинты

#### Базовые эндпоинты парсера:

| Метод | Эндпоинт | Описание |
| :--- | :--- | :--- |
| **GET** | `/api/v1/sources/files` | Возвращает список доступных CSV-файлов. |
| **GET** | `/api/v1/sources/file-info` | Получает метаданные о файле-источнике. |
| **POST** | `/api/v1/parse/from-file` | Запускает задачу парсинга из указанного файла. |
| **GET** | `/api/v1/search/channels` | Ищет публичные каналы по ключевому слову. |
| **POST** | `/api/v1/parse/from-search` | Запускает задачу парсинга из списка объектов каналов. |
| **GET** | `/api/v1/tasks/status/<task_id>`| Получает из Redis актуальный статус и прогресс задачи. |
| **GET** | `/api/v1/tasks/list` | Возвращает список всех недавних задач. |
| **POST** | `/api/v1/tasks/clear-completed` | Очищает завершенные задачи из Redis. |
| **GET** | `/api/stats` | Получает общую статистику парсера (сообщения, каналы). |
| **GET** | `/api/channels` | Получает список всех каналов с количеством сообщений. |
| **GET** | `/api/session/status` | Проверяет статус сессии Telegram. |

#### Pro-режим эндпоинты (требуют аутентификации):

| Метод | Эндпоинт | Описание |
| :--- | :--- | :--- |
| **GET** | `/pro/api/stats` | Общая статистика Pro-режима (сообщения, события, темы). |
| **GET** | `/pro/api/model/info` | Информация о моделях и коллекциях Qdrant. |
| **POST** | `/pro/api/topic-modeling/run` | Запуск пайплайна тематического моделирования. |
| **GET** | `/pro/api/topic-modeling/settings` | Получить настройки тематического моделирования. |
| **POST** | `/pro/api/topic-modeling/settings` | Сохранить настройки тематического моделирования. |
| **POST** | `/pro/api/topic-modeling/classification/run` | Ручной запуск классификации сообщений. |
| **GET** | `/pro/api/search` | Семантический поиск сообщений. |
| **GET** | `/pro/api/trends` | Получить трендовые события с обнаружением всплесков. |
| **GET** | `/pro/api/feed` | Персонализированная лента сообщений. |
| **GET** | `/pro/api/events/<cluster_id>` | Детали конкретного события/кластера. |
| **GET** | `/pro/api/topics` | Список всех тем. |
| **POST** | `/pro/api/topics/replace-universal` | Заменить темы на универсальный список. |

-----

## 📂 Детальный обзор структуры проекта

  - **Корневая директория**:

      - `app.py`: Веб-сервер Flask и API.
      - `tasks.py`: Определения задач Huey.
      - `huey_consumer.py`: Скрипт для запуска фонового обработчика.
      - `setup_main_session.py`: Скрипт для аутентификации.
      - `init_db.py`: Скрипт для инициализации БД.
      - `config.ini`, `.env`: Файлы конфигурации.
      - `requirements.txt`: Зависимости Python.
      - `*.session`: Файлы сессий. Рекомендуется добавить в `.gitignore`.

  - **`parser_app/`**: Пакет с основной логикой взаимодействия с Telegram.

      - `telegram_client_manager.py`: Управляет клиентами Pyrogram.
      - `channel_parser.py`: Логика получения истории сообщений.
      - `channel_searcher.py`: Логика поиска каналов.
      - `message_processor.py`: Преобразует сообщения в формат для БД.
      - `data_writer.py`: Обрабатывает запись данных в PostgreSQL.
      - `channel_provider.py`: Логика чтения каналов из CSV-файлов.

  - **`pro_mode/`**: Пакет Pro-режима с продвинутой аналитикой.

      - `__init__.py`: Основной сервис координации всех функций Pro-режима.
      - `api.py`: Flask API endpoints для Pro-режима.
      - `embedding_service.py`: Работа с эмбеддингами (FRIDA, GTE, Qdrant).
      - `topic_modeling_service.py`: Тематическое моделирование через BERTopic.
      - `classification_service.py`: Классификация сообщений по темам.
      - `deduplication_service.py`: Дедупликация и создание событий.
      - `openai_title_generator.py`: Генерация заголовков через OpenAI GPT.
      - `tasks_pro.py`: Фоновые задачи Huey для Pro-режима.

  - **`auth/`**: Пакет системы аутентификации.

      - `routes.py`: API эндпоинты для аутентификации.
      - `user_service.py`: Управление пользователями.
      - `jwt.py`: Генерация и проверка JWT токенов.
      - `dependencies.py`: Декораторы для защиты роутов.

  - **`migrations/`**: Скрипты миграций базы данных.

  - **`DB_process/`**: Пакет с утилитами для офлайн-обработки данных.

  - **`channel_sources/`**: Директория по умолчанию для входных CSV-файлов.

  - **`artifacts/topic_modeling/`**: Автоматически формируемые отчеты тематического моделирования.

  - **`static/` & `templates/`**: Стандартные директории Flask для фронтенда.

  - **`docs/`**: Документация проекта.

-----

## 📚 Дополнительная документация

Подробное описание функционала приложения доступно в файле:
- **`docs/APPLICATION_FEATURES_DESCRIPTION_RU.md`** - полное описание всех функций, алгоритмов и технологий

Другие документы:
- **`docs/AUTH_JWT_FLOW_RU.md`** - процесс работы с JWT токенами
- **`docs/USER_MANAGEMENT_RU.md`** - управление пользователями
- **`docs/TOPIC_MODELING_SERVICE_RU.md`** - документация по тематическому моделированию
- **`docs/FRIDA_CATEGORIZE_TOPIC_ANALYSIS_RU.md`** - анализ использования categorize_topic 