# 🐳 Быстрый старт с Docker

Краткая инструкция по запуску приложения с Docker.

## Быстрая установка

1. **Клонируйте проект:**
```bash
git clone https://github.com/your-username/telegram_parser.git
cd telegram_parser
```

2. **Создайте `.env` файл:**
```bash
cp .env.example .env
nano .env  # Заполните все необходимые переменные
```

3. **Создайте Telegram сессию локально:**
```bash
python setup_main_session.py
# Скопируйте telegram_parser.session на сервер
```

4. **Запустите приложение:**
```bash
docker-compose up -d
```

5. **Инициализируйте базу данных:**
```bash
docker-compose exec web python init_db.py
```

6. **Откройте в браузере:**
```
http://your-server-ip:5000
```

## Основные команды

```bash
# Запуск
docker-compose up -d

# Остановка
docker-compose down

# Просмотр логов
docker-compose logs -f

# Перезапуск сервиса
docker-compose restart web

# Выполнение команды в контейнере
docker-compose exec web python <script.py>
```

## Структура сервисов

- **web** - Flask веб-приложение (порт 5000)
- **worker** - Huey worker для фоновых задач

**Локальные сервисы (на том же сервере):**
- **PostgreSQL** - база данных (127.0.0.1:5432)
- **Redis** - для очередей (127.0.0.1:6379)
- **Qdrant** - векторная БД (127.0.0.1:6333)

**⚠️ ВАЖНО:** 
- PostgreSQL, Redis и Qdrant должны быть установлены и запущены на том же сервере
- Docker контейнеры используют `network_mode: host` для доступа к localhost сервисам
- Убедитесь, что в `.env` файле указаны правильные адреса локальных сервисов

## Подробная документация

См. [DEPLOYMENT.md](DEPLOYMENT.md) для полной инструкции по развертыванию.

