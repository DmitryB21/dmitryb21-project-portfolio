# 📊 Метрики для мониторинга и оптимизации

Документ описывает ключевые метрики, которые необходимо контролировать в пайплайне Telegram Parser для обеспечения производительности, надежности и качества работы системы.

---

## 🎯 Категории метрик

### 1. **Производительность API (Performance Metrics)**
### 2. **Надежность системы (Reliability Metrics)**
### 3. **Использование ресурсов (Resource Metrics)**
### 4. **Обработка задач (Task Processing Metrics)**
### 5. **Базы данных (Database Metrics)**
### 6. **Бизнес-метрики (Business Metrics)**
### 7. **Качество данных (Data Quality Metrics)**
### 8. **Безопасность (Security Metrics)**

---

## 1. 📈 Производительность API

### 1.1 Latency (Задержка ответа)

**Что измерять:**
- **P50 (медиана)**: 50% запросов выполняются быстрее этого значения
- **P95**: 95% запросов выполняются быстрее этого значения
- **P99**: 99% запросов выполняются быстрее этого значения
- **Max**: Максимальная задержка

**Целевые значения:**
```
✅ Отлично:  P50 < 100ms,  P95 < 500ms,  P99 < 1000ms
⚠️ Приемлемо: P50 < 200ms,  P95 < 1000ms, P99 < 2000ms
❌ Проблема:  P50 > 500ms,  P95 > 2000ms, P99 > 5000ms
```

**Критические эндпоинты:**
- `GET /api/stats` - должен быть < 200ms
- `GET /api/channels` - должен быть < 300ms
- `GET /pro/api/stats` - должен быть < 500ms
- `POST /pro/api/search/semantic` - должен быть < 1000ms
- `GET /api/v1/tasks/status/<task_id>` - должен быть < 50ms (Redis)

**Как улучшить:**
- Добавить кэширование для часто запрашиваемых данных
- Оптимизировать SQL-запросы (индексы, EXPLAIN ANALYZE)
- Использовать connection pooling для БД
- Асинхронная обработка тяжелых операций
- CDN для статических файлов

### 1.2 Throughput (Пропускная способность)

**Что измерять:**
- **RPS (Requests Per Second)**: Количество запросов в секунду
- **QPS (Queries Per Second)**: Количество запросов к БД в секунду
- **Concurrent Users**: Количество одновременных пользователей

**Целевые значения:**
```
✅ Отлично:  > 100 RPS
⚠️ Приемлемо: > 50 RPS
❌ Проблема:  < 20 RPS
```

**Как улучшить:**
- Горизонтальное масштабирование (несколько инстансов)
- Load balancing (nginx, HAProxy)
- Оптимизация кода (убрать N+1 queries)
- Использование async/await для I/O операций

### 1.3 Response Time по эндпоинтам

**Мониторинг отдельных эндпоинтов:**

| Эндпоинт | Целевое время | Критическое время |
|----------|---------------|-------------------|
| `GET /api/stats` | < 200ms | > 1000ms |
| `GET /api/channels` | < 300ms | > 2000ms |
| `POST /api/v1/parse/from-file` | < 100ms (создание задачи) | > 500ms |
| `GET /api/v1/tasks/status/<id>` | < 50ms | > 200ms |
| `POST /pro/api/search/semantic` | < 1000ms | > 5000ms |
| `POST /pro/api/topic-modeling/run` | < 200ms (создание задачи) | > 1000ms |

---

## 2. 🛡️ Надежность системы

### 2.1 Uptime (Время работы)

**Что измерять:**
- **Uptime %**: Процент времени доступности системы
- **MTBF (Mean Time Between Failures)**: Среднее время между сбоями
- **MTTR (Mean Time To Repair)**: Среднее время восстановления

**Целевые значения:**
```
✅ Отлично:  99.9% (8.76 часов простоя в год)
⚠️ Приемлемо: 99.5% (43.8 часов простоя в год)
❌ Проблема:  < 99% (> 87.6 часов простоя в год)
```

**Как улучшить:**
- Health checks и автоматический перезапуск
- Мониторинг и алерты
- Graceful shutdown
- Резервное копирование и восстановление

### 2.2 Error Rate (Частота ошибок)

**Что измерять:**
- **4xx Errors**: Ошибки клиента (неправильные запросы)
- **5xx Errors**: Ошибки сервера (критические)
- **Error Rate %**: Процент запросов с ошибками

**Целевые значения:**
```
✅ Отлично:  < 0.1% (5xx ошибок)
⚠️ Приемлемо: < 1% (5xx ошибок)
❌ Проблема:  > 5% (5xx ошибок)
```

**Критические ошибки для мониторинга:**
- `500 Internal Server Error` - критично
- `502 Bad Gateway` - проблемы с upstream
- `503 Service Unavailable` - перегрузка
- `504 Gateway Timeout` - таймауты

**Как улучшить:**
- Логирование всех ошибок с контекстом
- Sentry или аналоги для отслеживания ошибок
- Retry механизмы для временных сбоев
- Circuit breakers для внешних сервисов

### 2.3 Availability (Доступность сервисов)

**Что измерять:**
- **PostgreSQL**: Доступность БД
- **Redis**: Доступность кэша/очереди
- **Qdrant**: Доступность векторной БД
- **Telegram API**: Доступность внешнего API

**Целевые значения:**
```
✅ Все сервисы: > 99.5%
```

---

## 3. 💻 Использование ресурсов

### 3.1 CPU Usage

**Что измерять:**
- **CPU %**: Процент использования CPU
- **Load Average**: Средняя нагрузка (1min, 5min, 15min)
- **CPU per Container**: Использование CPU контейнерами

**Целевые значения:**
```
✅ Отлично:  < 70% (среднее), < 90% (пиковое)
⚠️ Приемлемо: < 85% (среднее), < 95% (пиковое)
❌ Проблема:  > 90% (среднее), > 95% (пиковое)
```

**Как улучшить:**
- Оптимизация алгоритмов
- Использование async/await для I/O
- Масштабирование (больше воркеров)
- Профилирование кода (cProfile, py-spy)

### 3.2 Memory Usage

**Что измерять:**
- **RAM %**: Процент использования памяти
- **Memory per Container**: Память на контейнер
- **Memory Leaks**: Рост памяти со временем
- **Swap Usage**: Использование swap (должно быть 0)

**Целевые значения:**
```
✅ Отлично:  < 70% (среднее), < 85% (пиковое)
⚠️ Приемлемо: < 85% (среднее), < 95% (пиковое)
❌ Проблема:  > 90% (среднее), использование swap
```

**Особое внимание:**
- ML модели (FRIDA, GTE) могут занимать много памяти
- Векторные данные в Qdrant
- Кэш в Redis

**Как улучшить:**
- Ограничение памяти для контейнеров
- Периодическая очистка кэша
- Использование генераторов вместо списков
- Мониторинг утечек памяти

### 3.3 Disk Usage

**Что измерять:**
- **Disk Space %**: Процент использования диска
- **Disk I/O**: Скорость чтения/записи
- **Disk Queue**: Очередь операций с диском

**Целевые значения:**
```
✅ Отлично:  < 70% использовано
⚠️ Приемлемо: < 85% использовано
❌ Проблема:  > 90% использовано
```

**Что занимает место:**
- Модели ML (FRIDA, GTE) - могут быть 2-5 GB
- База данных PostgreSQL
- Векторная БД Qdrant
- Логи приложения
- Сессии Telegram

**Как улучшить:**
- Ротация логов
- Очистка старых данных
- Сжатие данных
- Мониторинг роста БД

### 3.4 Network Usage

**Что измерять:**
- **Bandwidth**: Пропускная способность сети
- **Latency**: Задержка сети
- **Packet Loss**: Потеря пакетов

**Целевые значения:**
```
✅ Отлично:  < 80% пропускной способности
⚠️ Приемлемо: < 90% пропускной способности
❌ Проблема:  > 95% пропускной способности
```

---

## 4. ⚙️ Обработка задач (Huey Workers)

### 4.1 Task Queue Metrics

**Что измерять:**
- **Queue Length**: Количество задач в очереди
- **Queue Processing Rate**: Скорость обработки задач
- **Task Wait Time**: Время ожидания задачи в очереди
- **Task Execution Time**: Время выполнения задачи

**Целевые значения:**
```
✅ Отлично:  Queue Length < 100, Wait Time < 1min
⚠️ Приемлемо: Queue Length < 500, Wait Time < 5min
❌ Проблема:  Queue Length > 1000, Wait Time > 10min
```

**Типы задач для мониторинга:**
- Парсинг каналов (`orchestrate_parsing_from_file`)
- Индексация сообщений (`index_batch_task`)
- Тематическое моделирование (`run_topic_modeling_pipeline`)
- Классификация сообщений
- Дедипликация

### 4.2 Task Success Rate

**Что измерять:**
- **Success Rate %**: Процент успешно выполненных задач
- **Failed Tasks**: Количество провалившихся задач
- **Retry Rate**: Процент задач, требующих повтора

**Целевые значения:**
```
✅ Отлично:  > 99% успешных задач
⚠️ Приемлемо: > 95% успешных задач
❌ Проблема:  < 90% успешных задач
```

### 4.3 Worker Metrics

**Что измерять:**
- **Active Workers**: Количество активных воркеров
- **Worker Utilization**: Использование воркеров (%)
- **Worker Errors**: Ошибки воркеров

**Целевые значения:**
```
✅ Отлично:  Worker Utilization 60-80%
⚠️ Приемлемо: Worker Utilization 40-90%
❌ Проблема:  Worker Utilization < 30% или > 95%
```

**Как улучшить:**
- Автомасштабирование воркеров
- Приоритизация задач
- Балансировка нагрузки между воркерами
- Мониторинг и алерты при ошибках

---

## 5. 🗄️ Базы данных

### 5.1 PostgreSQL Metrics

**Что измерять:**
- **Connection Pool**: Использование пула соединений
- **Query Time**: Время выполнения запросов
- **Slow Queries**: Запросы > 1 секунды
- **Database Size**: Размер базы данных
- **Table Sizes**: Размеры таблиц
- **Index Usage**: Использование индексов
- **Cache Hit Ratio**: Процент попаданий в кэш (должен быть > 95%)

**Целевые значения:**
```
✅ Отлично:  
  - Cache Hit Ratio > 98%
  - Slow Queries < 1%
  - Connection Pool Usage < 80%

⚠️ Приемлемо:
  - Cache Hit Ratio > 95%
  - Slow Queries < 5%
  - Connection Pool Usage < 90%

❌ Проблема:
  - Cache Hit Ratio < 90%
  - Slow Queries > 10%
  - Connection Pool Exhausted
```

**Критические таблицы:**
- `messages` - может быть очень большой
- `channels` - растет со временем
- `events` - в Pro-режиме
- `topics` - в Pro-режиме

**Как улучшить:**
- Индексы на часто используемых полях
- Партиционирование больших таблиц
- VACUUM и ANALYZE регулярно
- Connection pooling (pgBouncer)
- Мониторинг медленных запросов

### 5.2 Redis Metrics

**Что измерять:**
- **Memory Usage**: Использование памяти
- **Hit Rate**: Процент попаданий в кэш
- **Connection Count**: Количество соединений
- **Commands per Second**: Команд в секунду
- **Key Count**: Количество ключей
- **Expired Keys**: Количество истекших ключей

**Целевые значения:**
```
✅ Отлично:  
  - Hit Rate > 90%
  - Memory Usage < 80%
  - Connection Count < 100

⚠️ Приемлемо:
  - Hit Rate > 70%
  - Memory Usage < 90%
  - Connection Count < 200

❌ Проблема:
  - Hit Rate < 50%
  - Memory Usage > 95%
  - Connection Count > 500
```

**Как улучшить:**
- Настройка TTL для ключей
- Использование Redis Cluster для больших объемов
- Мониторинг и очистка старых ключей
- Оптимизация структуры данных

### 5.3 Qdrant Metrics

**Что измерять:**
- **Collection Size**: Размер коллекций
- **Vector Count**: Количество векторов
- **Query Time**: Время поиска векторов
- **Index Status**: Статус индексации
- **Memory Usage**: Использование памяти

**Целевые значения:**
```
✅ Отлично:  
  - Query Time < 100ms (для 1000 результатов)
  - Index Status = "green"
  - Memory Usage < 80%

⚠️ Приемлемо:
  - Query Time < 500ms
  - Index Status = "yellow"
  - Memory Usage < 90%

❌ Проблема:
  - Query Time > 1000ms
  - Index Status = "red"
  - Memory Usage > 95%
```

**Как улучшить:**
- Оптимизация параметров индексации (HNSW)
- Регулярная оптимизация коллекций
- Мониторинг роста коллекций
- Очистка старых векторов

---

## 6. 📊 Бизнес-метрики

### 6.1 Парсинг данных

**Что измерять:**
- **Messages Parsed**: Количество спарсенных сообщений
- **Channels Parsed**: Количество обработанных каналов
- **Parsing Rate**: Скорость парсинга (сообщений/час)
- **Success Rate**: Процент успешных парсингов

**Целевые значения:**
```
✅ Отлично:  Success Rate > 99%
⚠️ Приемлемо: Success Rate > 95%
❌ Проблема:  Success Rate < 90%
```

### 6.2 Pro-режим метрики

**Что измерять:**
- **Events Created**: Количество созданных событий
- **Topics Identified**: Количество идентифицированных тем
- **Clusters Created**: Количество кластеров
- **Indexing Rate**: Скорость индексации (сообщений/час)
- **Search Queries**: Количество поисковых запросов
- **Search Success Rate**: Процент успешных поисков

**Целевые значения:**
```
✅ Отлично:  
  - Indexing Rate > 1000 сообщений/час
  - Search Success Rate > 95%

⚠️ Приемлемо:
  - Indexing Rate > 500 сообщений/час
  - Search Success Rate > 85%

❌ Проблема:
  - Indexing Rate < 100 сообщений/час
  - Search Success Rate < 70%
```

### 6.3 Пользовательская активность

**Что измерять:**
- **Active Users**: Количество активных пользователей
- **API Calls per User**: Количество вызовов API на пользователя
- **Feature Usage**: Использование функций (поиск, темы, экспорт)

---

## 7. 🔍 Качество данных

### 7.1 Data Completeness

**Что измерять:**
- **Missing Fields**: Процент записей с отсутствующими полями
- **Null Values**: Количество NULL значений
- **Data Freshness**: Актуальность данных (последнее обновление)

**Целевые значения:**
```
✅ Отлично:  < 1% missing fields
⚠️ Приемлемо: < 5% missing fields
❌ Проблема:  > 10% missing fields
```

### 7.2 Data Accuracy

**Что измерять:**
- **Embedding Quality**: Качество эмбеддингов (cosine similarity)
- **Classification Accuracy**: Точность классификации
- **Deduplication Rate**: Процент дубликатов

**Целевые значения:**
```
✅ Отлично:  
  - Classification Accuracy > 90%
  - Deduplication Rate < 1%

⚠️ Приемлемо:
  - Classification Accuracy > 80%
  - Deduplication Rate < 5%

❌ Проблема:
  - Classification Accuracy < 70%
  - Deduplication Rate > 10%
```

### 7.3 Data Consistency

**Что измерять:**
- **Referential Integrity**: Целостность связей между таблицами
- **Data Synchronization**: Синхронизация между PostgreSQL и Qdrant
- **Orphan Records**: Записи-сироты

---

## 8. 🔒 Безопасность

### 8.1 Authentication & Authorization

**Что измерять:**
- **Failed Login Attempts**: Количество неудачных попыток входа
- **Session Duration**: Длительность сессий
- **Token Expiration**: Использование истекших токенов

**Целевые значения:**
```
✅ Отлично:  Failed Logins < 10/час на IP
⚠️ Приемлемо: Failed Logins < 50/час на IP
❌ Проблема:  Failed Logins > 100/час на IP (возможна атака)
```

### 8.2 Security Events

**Что измерять:**
- **Unauthorized Access**: Неавторизованные попытки доступа
- **SQL Injection Attempts**: Попытки SQL-инъекций
- **Rate Limiting Triggers**: Срабатывания ограничения скорости

**Как улучшить:**
- Rate limiting на API
- WAF (Web Application Firewall)
- Мониторинг подозрительной активности
- Регулярные обновления зависимостей

---

## 🛠️ Инструменты для мониторинга

### Рекомендуемый стек:

1. **Prometheus + Grafana**
   - Метрики производительности
   - Визуализация дашбордов
   - Алерты

2. **Sentry**
   - Отслеживание ошибок
   - Performance monitoring
   - Release tracking

3. **ELK Stack (Elasticsearch, Logstash, Kibana)**
   - Централизованное логирование
   - Поиск по логам
   - Анализ трендов

4. **pgAdmin / pg_stat_statements**
   - Мониторинг PostgreSQL
   - Медленные запросы

5. **Redis Insight**
   - Мониторинг Redis
   - Анализ ключей

6. **Docker Stats / cAdvisor**
   - Метрики контейнеров
   - Использование ресурсов

---

## 📋 Чеклист для регулярного мониторинга

### Ежедневно:
- [ ] Проверка uptime и error rate
- [ ] Мониторинг использования ресурсов (CPU, RAM, Disk)
- [ ] Проверка очереди задач Huey
- [ ] Анализ критических ошибок

### Еженедельно:
- [ ] Анализ медленных запросов к БД
- [ ] Проверка роста размера БД
- [ ] Анализ бизнес-метрик
- [ ] Проверка качества данных

### Ежемесячно:
- [ ] Полный аудит производительности
- [ ] Оптимизация запросов
- [ ] Анализ трендов и планирование масштабирования
- [ ] Обзор безопасности

---

## 🎯 Приоритеты улучшения

### Критично (P0):
1. Uptime > 99.5%
2. Error Rate < 1%
3. P95 latency < 1000ms для критических эндпоинтов
4. Database connection pool не исчерпывается

### Важно (P1):
1. Queue Length < 500
2. CPU Usage < 85%
3. Memory Usage < 85%
4. Cache Hit Ratio > 95%

### Желательно (P2):
1. Search Success Rate > 90%
2. Indexing Rate > 500 сообщений/час
3. Classification Accuracy > 85%

---

## 📈 Метрики для дашборда

### Основной дашборд должен показывать:

1. **Общее состояние системы**
   - Uptime %
   - Error Rate %
   - Active Users

2. **Производительность**
   - P50, P95, P99 latency
   - RPS
   - Top медленные эндпоинты

3. **Ресурсы**
   - CPU, RAM, Disk usage
   - Network bandwidth

4. **Очередь задач**
   - Queue Length
   - Task Success Rate
   - Worker Utilization

5. **Базы данных**
   - PostgreSQL: Cache Hit Ratio, Slow Queries
   - Redis: Hit Rate, Memory Usage
   - Qdrant: Query Time, Collection Size

6. **Бизнес-метрики**
   - Messages Parsed (сегодня/неделя/месяц)
   - Events Created
   - Search Queries

---

## 🔄 Непрерывное улучшение

### Процесс:

1. **Измерение** → Собирайте метрики постоянно
2. **Анализ** → Определяйте узкие места
3. **Оптимизация** → Улучшайте проблемные области
4. **Валидация** → Проверяйте улучшения
5. **Повторение** → Цикл продолжается

### Золотое правило:
> **"You can't improve what you don't measure"**
> 
> Нельзя улучшить то, что не измеряется. Всегда измеряйте метрики перед оптимизацией!

