# Объяснение количества чанков

## Текущая ситуация

- **Документов:** 114
- **Чанков:** 8698
- **Среднее чанков на документ:** ~76
- **Размер чанка:** 300 токенов
- **Overlap:** 25% (75 токенов)

## Расчет

При размере чанка 300 токенов и overlap 25%:
- **Шаг между чанками:** 225 токенов (300 - 75)
- **Средний размер документа:** ~17,167 токенов (76 × 225)

## Почему так много чанков?

### 1. Настройки чанкинга

Текущие настройки (по умолчанию):
```python
chunk_size = 300 токенов
overlap_percent = 0.25 (25%)
```

Эти настройки можно изменить через переменные окружения в `.env`:
```env
CHUNK_SIZE=300
CHUNK_OVERLAP_PERCENT=0.25
```

### 2. Особенности алгоритма

Алгоритм чанкинга:
- Использует приблизительную оценку: **1 токен ≈ 3 символа** для русского текста
- Ищет границы предложений для более чистого разрыва
- Это может создавать дополнительные чанки, если границы предложений находятся не в оптимальных местах

### 3. Размер документов

Если у вас большие документы (например, инструкции, руководства), то 76 чанков на документ может быть нормальным.

**Пример:**
- Документ на 17,167 токенов ≈ ~51,500 символов ≈ ~8,600 слов
- Это примерно 17-20 страниц текста

## Как уменьшить количество чанков?

### Вариант 1: Увеличить размер чанка

Увеличьте `CHUNK_SIZE` в `.env`:

```env
CHUNK_SIZE=500  # Вместо 300
CHUNK_OVERLAP_PERCENT=0.25
```

**Эффект:**
- При chunk_size=500 и overlap=25%: шаг = 375 токенов
- Для документа на 17,167 токенов: ~46 чанков вместо 76
- Для 114 документов: ~5,244 чанков вместо 8,698

### Вариант 2: Уменьшить overlap

Уменьшите `CHUNK_OVERLAP_PERCENT`:

```env
CHUNK_SIZE=300
CHUNK_OVERLAP_PERCENT=0.15  # Вместо 0.25
```

**Эффект:**
- При chunk_size=300 и overlap=15%: шаг = 255 токенов
- Для документа на 17,167 токенов: ~67 чанков вместо 76
- Для 114 документов: ~7,638 чанков вместо 8,698

### Вариант 3: Комбинация (рекомендуется)

```env
CHUNK_SIZE=400
CHUNK_OVERLAP_PERCENT=0.20
```

**Эффект:**
- Шаг = 320 токенов
- Для документа на 17,167 токенов: ~54 чанка
- Для 114 документов: ~6,156 чанков

## Рекомендации по выбору размера чанка

### Малые чанки (200-300 токенов)
- ✅ Более точное извлечение информации
- ✅ Меньше "шумного" контекста
- ❌ Больше чанков → больше запросов к API
- ❌ Может потерять контекст длинных рассуждений

### Средние чанки (300-500 токенов)
- ✅ Баланс между точностью и контекстом
- ✅ Подходит для большинства случаев
- ✅ Оптимально для RAG систем

### Большие чанки (500-1000 токенов)
- ✅ Больше контекста в одном чанке
- ✅ Меньше чанков → меньше запросов
- ❌ Может содержать нерелевантную информацию
- ❌ Требует больше токенов для LLM

## Проверка текущих настроек

Проверьте настройки в `.env`:

```bash
# Текущие значения
grep CHUNK .env
```

Если переменных нет, используются значения по умолчанию (chunk_size=300, overlap=0.25).

## Влияние на производительность

Больше чанков означает:
- ⏱️ Больше времени на генерацию embeddings
- 💰 Больше запросов к GigaChat API (если используется платный план)
- 📊 Больше данных в Qdrant
- ✅ Но лучшее качество поиска (более точное извлечение)

Меньше чанков означает:
- ⚡ Быстрее индексация
- 💰 Меньше запросов к API
- 📊 Меньше данных в Qdrant
- ❌ Но может быть менее точный поиск

## Вывод

**8698 чанков из 114 документов** — это нормально для:
- Больших документов (в среднем ~17,000 токенов каждый)
- Размера чанка 300 токенов
- Overlap 25%

Если хотите уменьшить количество чанков:
1. Увеличьте `CHUNK_SIZE` до 400-500
2. Уменьшите `CHUNK_OVERLAP_PERCENT` до 0.15-0.20
3. Или используйте комбинацию обеих настроек

