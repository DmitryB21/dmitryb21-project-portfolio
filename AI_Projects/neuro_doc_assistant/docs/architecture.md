🧠 Проект: Neuro_Doc_Assistant
RAG + Lightweight AI-Agent для внутренней документации (GigaChain focus)
🎯 1. Цель проекта

Создание production-ready RAG-ассистента в формате AI-агента для работы с внутренней документацией компании, ориентированного на GigaChain-подход: агентность, инструменты (tools), микросервисная архитектура, измеримость качества и готовность к промышленной эксплуатации.

Проект демонстрирует умение:
проектировать AI-агентов поверх LLM;
интегрировать LLM в бизнес-процессы через API;
проводить R&D-эксперименты и измерять эффект;
доводить прототип до production-ready состояния.

⚙️ 2. Архитектура и технологический стек

Проект полностью развёрнут в SberCloud с использованием российских моделей и сервисов.
Категория	Технология	Роль
LLM	GigaChat API	Генерация ответов, reasoning, tool-calling
Agent Framework	Lightweight Agent Layer (custom / LangChain optional)	Оркестрация инструментов и шагов рассуждения
Embeddings	FRIDA / GigaChat Embeddings	Семантический поиск по русскоязычным данным
Vector DB	Qdrant (self-hosted)	Retrieval документов
Backend	FastAPI, AsyncIO	REST API, agent endpoint
Storage	SberCloud Object Storage	Хранение документов
Metadata DB	Managed PostgreSQL	Метаданные, эксперименты, логи
UI (Demo)	Streamlit / Gradio	Chat-интерфейс + метрики
Evaluation	RAGAS	Автоматическая оценка качества
Monitoring	Prometheus + Grafana	Latency, QPS, ошибки

🧠 3. Agent Layer (Lightweight)

Ассистент реализован как LLM-агент с доступом к инструментам, а не как жёстко зашитый RAG-пайплайн.

🎛 Доступные инструменты агента
Tool	Назначение
semantic_search	Поиск релевантных чанков в Qdrant
metadata_filter	Фильтрация по источнику, дате, типу документа
rerank_documents	Переупорядочивание retrieval-результатов
generate_answer	Генерация ответа на основе контекста
log_metrics	Логирование latency и качества

Агент принимает решение какие инструменты вызывать и в каком порядке, что приближает архитектуру к реальным AI-агентам GigaChain.

Реализация возможна как через LangChain / LlamaIndex, так и через собственную минималистичную agent-abstraction.

📏 4. Метрики и измеримость (ключевой фокус)

Проект изначально спроектирован как измеримая AI-система, а не «чат с LLM».

🎯 Целевые метрики
Метрика	Цель
End-to-end latency (p95)	< 1.3 сек
Retrieval latency (p95)	< 200 мс
Precision@3	≥ 80%
RAG Faithfulness (RAGAS)	≥ 0.85
Answer Relevancy (RAGAS)	≥ 0.80
Indexing throughput	≥ 1200 док/час
🔬 5. Экспериментальный цикл (R&D focus)

Проект включает экспериментальный цикл оценки влияния компонентов RAG на качество ответа.

📊 Experimental Loop

Каждая конфигурация фиксируется и сравнивается по метрикам качества и latency.

Примеры экспериментов:

Эксперимент	Цель
Размер чанков (200 / 300 / 400 токенов)	Влияние на Faithfulness и Recall
Количество retrieved документов (K=3/5/8)	Баланс Precision vs Latency
С reranking / без reranking	Прирост Precision@3
Разные embedding-модели	Качество retrieval

Результаты экспериментов:

логируются в PostgreSQL;
визуализируются в Grafana;
используются для выбора оптимальной конфигурации агента.

🧪 6. Error Analysis

Для ответов с низким Faithfulness или Relevancy проводится ручной и автоматический error analysis:

нерелевантные чанки;
недостаточный контекст;
частичные галлюцинации;
проблемы чанкинга.

Выводы используются для корректировки пайплайна и agent-логики.

🚀 7. Деплой и эксплуатация

Docker + GitHub Actions (CI/CD)
FastAPI и Qdrant — Compute Cloud VM
PostgreSQL — Managed Service
Prometheus + Grafana — мониторинг
UI-демо отображает:
ответ агента;
использованные источники;
текущие метрики качества и latency.

👤 8. Роль в проекте

Роль: AI Engineer / LLM Engineer (GigaChain-oriented)

Результат:
Разработал AI-агента для корпоративной документации на базе GigaChat с agent-layer, инструментами и экспериментальным циклом оценки качества. Реализовал RAG, мониторинг, RAGAS-оценку и серию экспериментов по оптимизации retrieval и generation. Достиг Faithfulness ≥ 0.85, Precision@3 ≥ 80%.

🧩 9. Сценарии применения (Use Cases) и тест-кейсы

Ниже приведены практические сценарии использования системы после завершения проекта. Они служат основой для проектирования архитектуры, Agent Layer и пошаговой реализации. Каждый сценарий можно напрямую трансформировать в integration / acceptance tests.

🟢 UC-1. Базовый поиск информации по документации

Описание: Сотрудник задаёт вопрос по внутренней документации (регламенты, API, инструкции).

Пример запроса:

«Какой SLA у сервиса платежей?»

Ожидаемое поведение агента:
Агент вызывает semantic_search.
Получает топ-3 релевантных чанка.
Генерирует ответ строго на основе найденных документов.
Возвращает ссылки на источники.

Acceptance Criteria:

Precision@3 ≥ 80%

Faithfulness ≥ 0.85

Ответ содержит ссылки на документы

🟢 UC-2. Ответ с уточнением контекста (Agent Reasoning)

Описание: Вопрос пользователя неоднозначен или слишком общий.

Пример запроса:

«Какие есть лимиты?»

Ожидаемое поведение агента:

Агент анализирует недостаток контекста.
Запрашивает уточнение (тип лимитов, система).
После уточнения повторяет retrieval и генерацию.

Acceptance Criteria:

Агент не галлюцинирует
Генерация уточняющего вопроса без вызова retrieval

🟢 UC-3. Фильтрация по метаданным

Описание: Пользователь ищет информацию в определённом источнике или периоде.

Пример запроса:

«Что сказано про rate limits в документации API за 2024 год?»

Ожидаемое поведение агента:

Агент вызывает metadata_filter.
Выполняет semantic_search по отфильтрованным данным.
Генерирует ответ.

Acceptance Criteria:

Используются только документы, соответствующие фильтру
Корректные ссылки на источники

🟢 UC-4. Reranking при большом объёме данных

Описание: В системе много похожих документов.

Ожидаемое поведение агента:

Retrieval с K=8.
Вызов rerank_documents.
Использование топ-3 после reranking.

Acceptance Criteria:

Precision@3 выше, чем без reranking
Latency увеличивается не более чем на 10–15%

🟢 UC-5. Обработка отсутствия информации

Описание: Документация не содержит ответа.

Пример запроса:

«Когда будет запущен продукт X?»

Ожидаемое поведение агента:

Retrieval возвращает нерелевантные документы.
Агент корректно сообщает об отсутствии информации.

Acceptance Criteria:

Отсутствие галлюцинаций
Явное указание, что информации нет в документации

🟢 UC-6. Экспериментальный сценарий (R&D)

Описание: ML-инженер запускает серию экспериментов для улучшения качества RAG.

Пример:

chunk_size = 200 / 300 / 400
K = 3 / 5 / 8

Ожидаемое поведение:

Конфигурации задаются через config.
Метрики автоматически рассчитываются.
Результаты сохраняются в PostgreSQL.

Acceptance Criteria:

Повторяемость экспериментов
Возможность сравнения метрик

🟢 UC-7. Мониторинг в продакшене

Описание: Система используется реальными пользователями.

Ожидаемое поведение:

Prometheus собирает latency, QPS, errors
Grafana отображает p95 latency и quality trends

Acceptance Criteria:

End-to-end latency p95 < 1.3 сек
Отсутствие критических ошибок

🟢 UC-8. Демонстрация для стейкхолдеров

Описание: Проект демонстрируется как MVP.

Ожидаемое поведение:

UI показывает ответ, источники и метрики
Возможность переключения конфигураций

Acceptance Criteria:

Наглядность
Повторяемость сценариев

🧱 10. Использование сценариев для реализации

Рекомендуемый порядок разработки:

UC-1 → базовый RAG
UC-5 → защита от галлюцинаций
UC-3 → метаданные
UC-4 → reranking
UC-6 → экспериментальный цикл
UC-7 → мониторинг
UC-8 → polishing

Такой подход позволяет идти от тест-кейсов к архитектуре, что соответствует лучшим практикам enterprise-разработки AI-систем.

🛠 11. План пошаговой реализации (Test-driven → Modules → Agent)

Проект реализуется по принципу test-first / use-case-driven development: сначала формулируются тест-кейсы (на основе UC), затем проектируются модули и только после этого — код.

🧪 11.1. Этап 0 — Формализация тест-кейсов (основа всего проекта)

Для каждого Use Case формируются acceptance / integration tests.

Пример шаблона тест-кейса (Given / When / Then)

UC-1: Базовый поиск

Given: документация про SLA загружена и проиндексирована

When: пользователь задаёт вопрос «Какой SLA у сервиса платежей?»

Then:

Precision@3 ≥ 80%

Faithfulness ≥ 0.85

ответ содержит ссылки на источники

Все тест-кейсы фиксируются в /tests/use_cases/ и используются как критерий готовности этапов.

🧱 11.2. Этап 1 — Модульная декомпозиция системы
📦 Модуль 1. Ingestion & Indexing

Задачи:

загрузка документов (PDF, DOCX, MD, TXT)
парсинг и нормализация текста
чанкинг (200–400 токенов)
генерация embeddings
загрузка в Qdrant

Артефакты:

DocumentLoader
Chunker
EmbeddingService
QdrantIndexer

Покрываемые UC: UC-1, UC-6

📦 Модуль 2. Metadata & Storage

Задачи:

хранение метаданных документов
хранение конфигураций экспериментов
логирование результатов

Артефакты:

MetadataRepository
ExperimentRepository
Покрываемые UC: UC-3, UC-6

📦 Модуль 3. Retrieval Layer

Задачи:

semantic search в Qdrant
фильтрация по метаданным
параметризация K

Артефакты:

Retriever
MetadataFilter
Покрываемые UC: UC-1, UC-3, UC-5

📦 Модуль 4. Reranking (опциональный, но важный)

Задачи:

переупорядочивание retrieved документов
сравнение результатов с / без reranking

Артефакты:

Reranker
Покрываемые UC: UC-4, UC-6

📦 Модуль 5. Generation Layer

Задачи:

формирование prompt
вызов GigaChat API
строгая инструкция «отвечай только по контексту»

Артефакты:

PromptBuilder
LLMClient
Покрываемые UC: UC-1, UC-5

📦 Модуль 6. Evaluation & Metrics

Задачи:

расчёт Precision@K
интеграция RAGAS
сбор latency / throughput

Артефакты:

MetricsCollector
RAGASEvaluator
Покрываемые UC: UC-6, UC-7

📦 Модуль 7. Agent Layer (Lightweight, Deterministic)
Назначение

Единственный модуль, принимающий решения.
Реализует контролируемый, воспроизводимый execution flow.

Задачи

оркестрация модулей
выбор: reranking / no reranking
управление экспериментальными прогонами
фиксация decision path

Артефакты

AgentController — основной orchestrator
AgentStateMachine — конечный автомат состояний
DecisionLog — трассировка решений агента

Покрываемые UC
UC-1 (основной сценарий)
UC-4 (сравнение конфигураций)
UC-6 (экспериментальный цикл)

📦 Модуль 8. API & UI Layer
Назначение

Тонкий слой взаимодействия с пользователем и внешними системами.

Задачи

REST API (FastAPI)
demo UI (Streamlit)
отображение источников и метрик
Артефакты
QueryAPI
AdminAPI
DemoUI

Покрываемые UC
UC-1, UC-2, UC-7


Структура репозитория

neuro-doc-assistant/
│
├── README.md
├── pyproject.toml
├── .env.example
├── docker-compose.yml
│
├── app/
│   ├── main.py                  # FastAPI entrypoint
│   │
│   ├── agent/                   # AGENT CORE
│   │   ├── agent.py
│   │   ├── state_machine.py
│   │   ├── decision_logic.py
│   │   └── tools.py
│   │
│   ├── ingestion/
│   │   ├── loader.py
│   │   ├── chunker.py
│   │   └── indexer.py
│   │
│   ├── retrieval/
│   │   ├── retriever.py
│   │   └── metadata_filter.py
│   │
│   ├── reranking/
│   │   └── reranker.py
│   │
│   ├── generation/
│   │   ├── prompt_builder.py
│   │   └── gigachat_client.py
│   │
│   ├── evaluation/
│   │   ├── ragas_evaluator.py
│   │   └── metrics.py
│   │
│   ├── monitoring/
│   │   └── prometheus.py
│   │
│   └── api/
│       ├── chat.py
│       └── health.py
│
├── tests/
│   ├── use_cases/
│   │   ├── test_uc1_basic_search.py
│   │   ├── test_uc3_metadata_filter.py
│   │   └── test_uc5_no_answer.py
│   │
│   ├── fixtures/
│   │   ├── vector_store.py
│   │   └── agent_client.py
│   │
│   └── conftest.py
│
├── docs/
│   ├── architecture.md
│   ├── agent_flow.md
│   └── experiments.md
│
└── .cursor/
    └── rules.md