# Процесс индексации документов

## Обзор

Индексация документов в Neuro_Doc_Assistant проходит в 5 основных этапов:

1. **Подключение к Qdrant** - установка соединения с векторной базой данных
2. **Инициализация компонентов** - настройка всех необходимых сервисов
3. **Загрузка документов** - чтение Markdown файлов из директорий `data/NeuroDoc_Data/hr/` и `data/NeuroDoc_Data/it/`
4. **Чанкинг** - разбиение документов на фрагменты (chunks) с overlap
5. **Генерация embeddings и индексация** - создание векторных представлений и сохранение в Qdrant

---

## Детальное описание этапов

### Этап 1: Подключение к Qdrant

```python
qdrant_client = QdrantClient(url="http://localhost:6333")
```

- Проверяется доступность Qdrant сервера
- Если Qdrant недоступен, процесс останавливается с ошибкой

### Этап 2: Инициализация компонентов

Инициализируются следующие компоненты:

1. **DocumentLoader** - загрузчик документов
2. **Chunker** - разбиение на чанки
3. **EmbeddingService** - генерация векторных представлений
4. **QdrantIndexer** - индексация в Qdrant

**Важно:** `EmbeddingService` может работать в двух режимах:

- **Реальный режим** - использует GigaChat Embeddings API (требует `GIGACHAT_API_KEY`)
- **Mock режим** - генерирует детерминированные векторы на основе MD5 hash текста

**Определение режима:**
```python
use_mock_mode = not gigachat_api_key or os.getenv("GIGACHAT_MOCK_MODE", "false").lower() == "true"
```

### Этап 3: Загрузка документов

```python
loader = DocumentLoader()
hr_documents = loader.load_documents("data/NeuroDoc_Data/hr/")
it_documents = loader.load_documents("data/NeuroDoc_Data/it/")
```

- Загружаются все `.md` файлы из указанных директорий
- Каждый документ получает уникальный `doc_id` на основе имени файла
- Определяется категория (`hr` или `it`) на основе префикса файла или директории
- Извлекаются метаданные (название, категория, путь к файлу)

**Результат:** Список объектов `Document` с текстом и метаданными

### Этап 4: Чанкинг

```python
chunker = Chunker()
chunks = chunker.chunk_documents(documents, chunk_size=300, overlap_percent=0.25)
```

**Параметры:**
- `chunk_size=300` - размер чанка в токенах (по умолчанию 300)
- `overlap_percent=0.25` - перекрытие между чанками 25% (75 токенов)

**Процесс:**
1. Текст документа токенизируется (используется `tiktoken` с кодировкой `cl100k_base`)
2. Документ разбивается на чанки с учетом overlap
3. Каждый чанк получает уникальный `chunk_id` в формате: `{doc_id}_chunk_{index:03d}`
4. Метаданные документа копируются в каждый чанк

**Результат:** Список объектов `Chunk` с текстом, ID и метаданными

### Этап 5: Генерация embeddings и индексация

#### 5.1. Генерация embeddings

```python
embedding_service = EmbeddingService(mock_mode=use_mock_mode)
embeddings = embedding_service.generate_embeddings(chunk_texts)
```

**Реальный режим (GigaChat API):**
- Отправляется POST запрос к `https://api.gigachat.ai/v1/embeddings`
- Заголовки: `Authorization: Bearer {api_key}`
- Payload: `{"model": "GigaChat", "input": "текст чанка"}`
- Получается вектор размерности 1536

**Mock режим:**
- Генерируется MD5 hash текста
- На основе hash создается детерминированный вектор размерности 1536
- Значения нормализуются в диапазон [-1, 1]
- **Важно:** Mock embeddings не отражают семантическое сходство текстов!

**Батчинг:**
- Embeddings генерируются батчами по 10 чанков для оптимизации
- Прогресс отображается в консоли

#### 5.2. Индексация в Qdrant

```python
indexer = QdrantIndexer(qdrant_client, collection_name="neuro_docs")
indexer.index_chunks(chunks, embeddings)
```

**Процесс:**

1. **Создание коллекции** (если не существует):
   ```python
   collection_name = "neuro_docs"
   vector_size = 1536
   distance = Distance.COSINE
   ```

2. **Преобразование chunk_id в числовой ID:**
   - Qdrant требует числовые ID (unsigned integer)
   - Строковый `chunk_id` преобразуется в числовой через MD5 hash:
   ```python
   point_id = int(hashlib.md5(chunk.chunk_id.encode('utf-8')).hexdigest()[:15], 16) % (2**63 - 1)
   ```

3. **Создание точек (PointStruct):**
   ```python
   point = PointStruct(
       id=point_id,  # Числовой ID
       vector=embedding,  # Вектор размерности 1536
       payload={
           "text": chunk.text,
           "doc_id": chunk.doc_id,
           "chunk_id": chunk.chunk_id,  # Оригинальный строковый ID
           "source": "hr" или "it",
           "category": "hr" или "it",
           "created_at": "2024-12-21T...",
           "text_length": 352,
           "embedding_version": "GigaChat",
           "metadata_tags": [],
           "file_path": "..."
       }
   )
   ```

4. **Загрузка в Qdrant:**
   ```python
   qdrant_client.upsert(collection_name="neuro_docs", points=[...])
   ```

---

## Текущая конфигурация

### Параметры индексации

- **Размерность векторов:** 1536
- **Размер чанка:** 300 токенов
- **Overlap:** 25% (75 токенов)
- **Метрика расстояния:** Cosine similarity
- **Коллекция:** `neuro_docs`

### Модель embeddings

**Текущий статус:** 
- API ключ установлен в `.env`
- `GIGACHAT_MOCK_MODE=false`
- **НО:** При ошибках подключения к API автоматически используется mock mode

**Рекомендации:**

1. **Для разработки и тестирования:**
   - Можно использовать mock mode (`GIGACHAT_MOCK_MODE=true`)
   - Mock embeddings детерминированы, но не отражают семантику

2. **Для production:**
   - Обязательно использовать реальный GigaChat Embeddings API
   - Убедиться, что `GIGACHAT_API_KEY` валиден
   - Проверить доступность `https://api.gigachat.ai`

---

## Проверка статуса индексации

Для проверки статуса индексации используйте скрипт:

```bash
python scripts/check_indexing_status.py
```

Скрипт покажет:
- Количество проиндексированных точек
- Примеры документов
- Конфигурацию EmbeddingService
- Режим работы (mock или реальный API)

---

## Пример вывода индексации

```
================================================================================
Neuro_Doc_Assistant - Ingestion Pipeline
================================================================================

[Шаг 1/5] Подключение к Qdrant...
✅ Подключение к Qdrant успешно: http://localhost:6333

[Шаг 2/5] Инициализация компонентов...
✅ Компоненты инициализированы (collection: neuro_docs, embedding_dim: 1536)

[Шаг 3/5] Загрузка документов...
   Загрузка HR документов из .../data/NeuroDoc_Data/hr...
   ✅ Загружено HR документов: 50
   Загрузка IT документов из .../data/NeuroDoc_Data/it...
   ✅ Загружено IT документов: 50
✅ Всего загружено документов: 100

[Шаг 4/5] Разбиение документов на чанки...
✅ Создано чанков: 450 (chunk_size=300, overlap=25.0%)

[Шаг 5/5] Генерация embeddings и индексация в Qdrant...
   Это может занять некоторое время...
   Обработано 10/450 чанков...
   ...
   Обработано 450/450 чанков...
✅ Сгенерировано embeddings: 450

   Индексация в Qdrant (коллекция: neuro_docs)...
   Коллекция будет создана автоматически, если не существует.
✅ Индексация завершена успешно!

================================================================================
✅ INGESTION PIPELINE ЗАВЕРШЕН УСПЕШНО
================================================================================

📊 Статистика:
   - Загружено документов: 100
   - Создано чанков: 450
   - Индексировано в Qdrant: 450
   - Коллекция: neuro_docs
```

---

## Устранение проблем

### Ошибка: "Failed to resolve 'api.gigachat.ai'"

**Причина:** Проблемы с DNS или отсутствие интернет-соединения

**Решение:** 
- Проверить интернет-соединение
- Использовать mock mode: `GIGACHAT_MOCK_MODE=true` в `.env`
- Перезапустить ingestion pipeline

### Ошибка: "Format error in JSON body: value ... is not a valid point ID"

**Причина:** Qdrant не принимает строковые ID

**Решение:** Уже исправлено - строковые `chunk_id` автоматически преобразуются в числовые ID через MD5 hash

### Ошибка: "Collection 'neuro_docs' not found"

**Причина:** Коллекция не была создана

**Решение:** Запустить ingestion pipeline - коллекция создастся автоматически

---

## Следующие шаги

После успешной индексации:

1. Запустить FastAPI сервер: `python app/main.py`
2. Запустить Streamlit UI: `streamlit run app/ui/streamlit_app.py`
3. Протестировать поиск через API или UI

