# Анализ соответствия проекта микросервисной архитектуре

## 📋 Содержание

1. [Критерии микросервисной архитектуры](#критерии-микросервисной-архитектуры)
2. [Текущая архитектура проекта](#текущая-архитектура-проекта)
3. [Детальный анализ по критериям](#детальный-анализ-по-критериям)
4. [Оценка соответствия](#оценка-соответствия)
5. [Рекомендации по миграции](#рекомендации-по-миграции)

---

## Критерии микросервисной архитектуры

### Основные принципы:

1. **Разделение на независимые сервисы** — каждый сервис имеет свою бизнес-логику и может работать автономно
2. **Независимое развертывание** — каждый сервис может быть развернут отдельно без влияния на другие
3. **Слабая связанность (Loose Coupling)** — сервисы взаимодействуют через четко определенные интерфейсы
4. **Высокая связность внутри сервиса (High Cohesion)** — связанная функциональность группируется в одном сервисе
5. **Независимые базы данных** — каждый сервис имеет свою БД или схему
6. **Коммуникация через API** — REST, gRPC, message queues, а не прямые вызовы функций
7. **Независимое масштабирование** — каждый сервис может масштабироваться независимо
8. **Отказоустойчивость** — падение одного сервиса не должно приводить к падению всей системы
9. **Service Discovery** — механизм обнаружения сервисов в runtime
10. **API Gateway** — единая точка входа для внешних клиентов

---

## Текущая архитектура проекта

### Структура проекта:

```
neuro_doc_assistant/
├── app/
│   ├── main.py              # FastAPI entrypoint (все модули в одном процессе)
│   ├── agent/               # Agent Layer
│   ├── ingestion/          # Ingestion & Indexing
│   ├── retrieval/           # Retrieval Layer
│   ├── reranking/           # Reranking
│   ├── generation/          # Generation Layer
│   ├── evaluation/           # Evaluation & Metrics
│   ├── monitoring/           # Monitoring
│   ├── storage/              # Storage
│   └── api/                  # API Layer
├── docker-compose.yml        # Развертывание (инфраструктура + один API сервис)
└── tests/
```

### Текущая архитектура развертывания:

```yaml
# docker-compose.yml
services:
  postgres:      # Инфраструктурный сервис (БД)
  minio:         # Инфраструктурный сервис (S3)
  qdrant:        # Инфраструктурный сервис (векторная БД)
  api:           # ЕДИНСТВЕННЫЙ бизнес-сервис (содержит все модули)
  streamlit:      # UI сервис
```

### Коммуникация между модулями:

**Текущий подход:** Прямые вызовы функций через Python imports

```python
# app/main.py
from app.agent.agent import AgentController
from app.retrieval.retriever import Retriever
from app.generation.gigachat_client import LLMClient
# ... все модули импортируются напрямую

# app/agent/agent.py
from app.retrieval.retriever import Retriever
from app.generation.prompt_builder import PromptBuilder
from app.generation.gigachat_client import LLMClient
# ... прямые вызовы методов
```

**Пример взаимодействия:**
```python
# AgentController напрямую вызывает методы других модулей
retrieved_chunks = self.retriever.retrieve(query, k=k)  # Прямой вызов
answer = self.llm_client.generate_answer(prompt)         # Прямой вызов
metrics = self.ragas_evaluator.evaluate_all(...)        # Прямой вызов
```

---

## Детальный анализ по критериям

### 1. Разделение на независимые сервисы

**Статус:** ❌ **НЕ СООТВЕТСТВУЕТ**

**Анализ:**
- Все модули (agent, ingestion, retrieval, generation, evaluation) находятся в **одном Python-процессе**
- Все модули загружаются в одном FastAPI приложении (`app/main.py`)
- Нет разделения на независимые сервисы

**Текущая структура:**
```
┌─────────────────────────────────────┐
│      FastAPI Application            │
│  ┌───────────────────────────────┐ │
│  │  Agent Layer                  │ │
│  │  Retrieval Layer              │ │
│  │  Generation Layer             │ │
│  │  Evaluation Layer             │ │
│  │  Ingestion Layer              │ │
│  │  Monitoring Layer             │ │
│  └───────────────────────────────┘ │
└─────────────────────────────────────┘
```

**Оценка:** 0/10 — все модули в одном процессе

---

### 2. Независимое развертывание

**Статус:** ❌ **НЕ СООТВЕТСТВУЕТ**

**Анализ:**
- Все модули развертываются вместе в одном Docker-контейнере (`api`)
- Изменение одного модуля требует пересборки и перезапуска всего приложения
- Нет возможности развернуть только один модуль отдельно

**Текущая ситуация:**
```yaml
# docker-compose.yml
api:  # Один контейнер содержит ВСЕ модули
  build:
    context: .
    dockerfile: Dockerfile
```

**Оценка:** 0/10 — нет независимого развертывания

---

### 3. Слабая связанность (Loose Coupling)

**Статус:** ⚠️ **ЧАСТИЧНО СООТВЕТСТВУЕТ**

**Анализ:**
- **Положительные аспекты:**
  - Модули разделены по функциональности (ingestion, retrieval, generation)
  - Используется dependency injection (модули передаются в AgentController через конструктор)
  - Четкие интерфейсы между модулями (Retriever, LLMClient, MetricsCollector)
  
- **Отрицательные аспекты:**
  - Коммуникация через прямые вызовы функций, а не через API
  - Модули знают о внутренней структуре друг друга (импорты классов)
  - Нет версионирования API между модулями

**Пример сильной связанности:**
```python
# app/agent/agent.py
from app.retrieval.retriever import Retriever  # Прямой импорт класса
from app.generation.gigachat_client import LLMClient  # Знание о конкретной реализации
```

**Оценка:** 3/10 — есть модульность, но нет слабой связанности через API

---

### 4. Высокая связность внутри сервиса (High Cohesion)

**Статус:** ✅ **СООТВЕТСТВУЕТ**

**Анализ:**
- Модули хорошо организованы по функциональности:
  - `ingestion/` — все компоненты для загрузки и индексации
  - `retrieval/` — все компоненты для поиска
  - `generation/` — все компоненты для генерации ответов
  - `evaluation/` — все компоненты для оценки качества
  
- Связанная функциональность сгруппирована вместе

**Оценка:** 9/10 — отличная организация модулей

---

### 5. Независимые базы данных

**Статус:** ⚠️ **ЧАСТИЧНО СООТВЕТСТВУЕТ**

**Анализ:**
- **Инфраструктурные БД разделены:**
  - PostgreSQL — метаданные документов и эксперименты
  - Qdrant — векторные представления документов
  - MinIO/S3 — исходные документы
  
- **Проблема:**
  - Все модули используют одни и те же БД
  - Нет изоляции данных между модулями
  - Нет возможности использовать разные БД для разных модулей

**Текущая ситуация:**
```
┌─────────────────┐
│  Agent Layer    │──┐
│  Retrieval      │  │
│  Generation     │  ├──► PostgreSQL (общая БД)
│  Evaluation     │  │
│  Ingestion      │──┘
└─────────────────┘
```

**Оценка:** 4/10 — БД разделены по типу, но не по модулям

---

### 6. Коммуникация через API

**Статус:** ❌ **НЕ СООТВЕТСТВУЕТ**

**Анализ:**
- **Текущая коммуникация:** Прямые вызовы функций через Python imports
- **Нет REST API между модулями:** Модули не общаются через HTTP/gRPC
- **Нет message queues:** Нет асинхронной коммуникации через очереди
- **Внешний API есть:** FastAPI предоставляет REST API для внешних клиентов, но не для внутренних модулей

**Пример текущей коммуникации:**
```python
# Прямой вызов функции (не микросервисный подход)
retrieved_chunks = self.retriever.retrieve(query, k=k)
answer = self.llm_client.generate_answer(prompt)
```

**Оценка:** 1/10 — есть внешний API, но нет внутреннего API между модулями

---

### 7. Независимое масштабирование

**Статус:** ❌ **НЕ СООТВЕТСТВУЕТ**

**Анализ:**
- Все модули масштабируются вместе (один контейнер = все модули)
- Невозможно масштабировать только retrieval или только generation отдельно
- Нет возможности горизонтального масштабирования отдельных модулей

**Текущая ситуация:**
```yaml
# Невозможно сделать так:
api-retrieval:
  replicas: 5  # Масштабировать только retrieval
api-generation:
  replicas: 3  # Масштабировать только generation
```

**Оценка:** 0/10 — нет независимого масштабирования

---

### 8. Отказоустойчивость

**Статус:** ⚠️ **ЧАСТИЧНО СООТВЕТСТВУЕТ**

**Анализ:**
- **Положительные аспекты:**
  - Есть обработка ошибок в AgentController
  - Есть health checks для внешних сервисов (Qdrant, GigaChat API)
  - Есть fallback на mock mode при недоступности внешних сервисов
  
- **Отрицательные аспекты:**
  - Падение одного модуля может привести к падению всего приложения
  - Нет изоляции отказов между модулями
  - Нет circuit breaker pattern для защиты от каскадных отказов

**Пример обработки ошибок:**
```python
# app/agent/agent.py
try:
    retrieved_chunks = self.retriever.retrieve(query, k=k)
except Exception as e:
    # Ошибка в retrieval приводит к возврату ошибки всего запроса
    return AgentResponse(answer=f"Ошибка: {str(e)}", ...)
```

**Оценка:** 4/10 — есть базовая обработка ошибок, но нет изоляции отказов

---

### 9. Service Discovery

**Статус:** ❌ **НЕ СООТВЕТСТВУЕТ**

**Анализ:**
- Нет механизма service discovery
- Модули знают о расположении друг друга через прямые импорты
- Нет динамического обнаружения сервисов в runtime
- Используется статическая конфигурация через переменные окружения

**Текущая ситуация:**
```python
# Статическая конфигурация
qdrant_url = os.getenv("QDRANT_URL", "http://localhost:6333")
qdrant_client = QdrantClient(url=qdrant_url)  # Жестко заданный URL
```

**Оценка:** 0/10 — нет service discovery

---

### 10. API Gateway

**Статус:** ⚠️ **ЧАСТИЧНО СООТВЕТСТВУЕТ**

**Анализ:**
- **Есть единая точка входа:** FastAPI приложение (`/ask`, `/admin/*`)
- **Нет полноценного API Gateway:**
  - Нет маршрутизации к разным сервисам
  - Нет rate limiting
  - Нет аутентификации/авторизации на уровне Gateway
  - Нет агрегации ответов от нескольких сервисов

**Текущая ситуация:**
```
Client → FastAPI (API Gateway?) → AgentController → Модули (прямые вызовы)
```

**Оценка:** 3/10 — есть единая точка входа, но нет функций Gateway

---

## Оценка соответствия

### Итоговая таблица:

| Критерий | Оценка | Статус |
|----------|--------|--------|
| 1. Разделение на независимые сервисы | 0/10 | ❌ |
| 2. Независимое развертывание | 0/10 | ❌ |
| 3. Слабая связанность | 3/10 | ⚠️ |
| 4. Высокая связность внутри сервиса | 9/10 | ✅ |
| 5. Независимые базы данных | 4/10 | ⚠️ |
| 6. Коммуникация через API | 1/10 | ❌ |
| 7. Независимое масштабирование | 0/10 | ❌ |
| 8. Отказоустойчивость | 4/10 | ⚠️ |
| 9. Service Discovery | 0/10 | ❌ |
| 10. API Gateway | 3/10 | ⚠️ |

**Общая оценка:** **2.4/10** (24%)

### Вывод:

Проект **НЕ соответствует микросервисной архитектуре**. Текущая архитектура — это **модульная монолитная архитектура** (Modular Monolith).

### Характеристики текущей архитектуры:

✅ **Модульная монолитная архитектура:**
- Хорошая модульность (разделение по функциональности)
- Четкие интерфейсы между модулями
- Dependency injection
- Единое развертывание
- Прямые вызовы функций между модулями

❌ **НЕ микросервисная архитектура:**
- Все модули в одном процессе
- Нет независимого развертывания
- Нет коммуникации через API
- Нет независимого масштабирования

---

## Рекомендации по миграции

### Вариант 1: Остаться на модульной монолитной архитектуре (рекомендуется)

**Преимущества:**
- ✅ Простота разработки и отладки
- ✅ Низкая латентность (прямые вызовы функций)
- ✅ Простое развертывание
- ✅ Легче обеспечить транзакционность
- ✅ Меньше операционных затрат

**Когда подходит:**
- Команда небольшая (< 10 человек)
- Нет необходимости в независимом масштабировании модулей
- Нет требований к изоляции отказов между модулями
- Проект находится на ранней стадии

**Рекомендация:** Для текущего проекта это оптимальный выбор.

---

### Вариант 2: Миграция к микросервисной архитектуре

Если есть реальная необходимость в микросервисах, вот план миграции:

#### Шаг 1: Выделение сервисов

```
Текущая структура → Микросервисы:

1. Ingestion Service
   - Endpoints: POST /ingestion/documents, GET /ingestion/status
   - БД: PostgreSQL (metadata), Qdrant (vectors)
   - Зависимости: GigaChat Embeddings API

2. Retrieval Service
   - Endpoints: POST /retrieval/search
   - БД: Qdrant (read-only)
   - Зависимости: GigaChat Embeddings API

3. Generation Service
   - Endpoints: POST /generation/answer
   - БД: нет (stateless)
   - Зависимости: GigaChat Chat API

4. Evaluation Service
   - Endpoints: POST /evaluation/metrics
   - БД: PostgreSQL (experiments)
   - Зависимости: RAGAS

5. Agent Service (Orchestrator)
   - Endpoints: POST /agent/ask
   - БД: нет (stateless)
   - Зависимости: Retrieval, Generation, Evaluation services

6. API Gateway
   - Endpoints: POST /ask, GET /admin/*
   - Функции: routing, rate limiting, auth
   - Зависимости: Agent Service
```

#### Шаг 2: Коммуникация между сервисами

**Варианты:**
- **REST API** (HTTP) — для синхронной коммуникации
- **gRPC** — для высокопроизводительной коммуникации
- **Message Queue** (RabbitMQ, Kafka) — для асинхронной коммуникации

**Пример:**
```python
# Agent Service вызывает Retrieval Service через HTTP
import requests

retrieval_response = requests.post(
    "http://retrieval-service:8001/search",
    json={"query": query, "k": k}
)
retrieved_chunks = retrieval_response.json()
```

#### Шаг 3: Service Discovery

**Варианты:**
- **Consul** — service discovery и configuration
- **Kubernetes Service Discovery** — если используете K8s
- **Eureka** — Netflix service discovery

#### Шаг 4: API Gateway

**Варианты:**
- **Kong** — open-source API Gateway
- **Traefik** — reverse proxy и load balancer
- **Nginx** — простой reverse proxy

#### Шаг 5: Развертывание

**Docker Compose для микросервисов:**
```yaml
services:
  ingestion-service:
    build: ./services/ingestion
    ports: ["8001:8000"]
  
  retrieval-service:
    build: ./services/retrieval
    ports: ["8002:8000"]
  
  generation-service:
    build: ./services/generation
    ports: ["8003:8000"]
  
  evaluation-service:
    build: ./services/evaluation
    ports: ["8004:8000"]
  
  agent-service:
    build: ./services/agent
    ports: ["8005:8000"]
    environment:
      - RETRIEVAL_SERVICE_URL=http://retrieval-service:8000
      - GENERATION_SERVICE_URL=http://generation-service:8000
  
  api-gateway:
    image: kong:latest
    ports: ["8000:8000"]
```

#### Шаг 6: Мониторинг и логирование

- **Distributed Tracing:** Jaeger, Zipkin
- **Centralized Logging:** ELK Stack, Loki
- **Metrics:** Prometheus + Grafana (уже есть)

---

### Сравнение архитектур

| Аспект | Модульный монолит | Микросервисы |
|--------|-------------------|--------------|
| **Сложность разработки** | Низкая | Высокая |
| **Сложность развертывания** | Низкая | Высокая |
| **Латентность** | Низкая (прямые вызовы) | Высокая (сеть) |
| **Масштабирование** | Вертикальное | Горизонтальное |
| **Отказоустойчивость** | Средняя | Высокая |
| **Операционные затраты** | Низкие | Высокие |
| **Тестирование** | Простое | Сложное (нужны моки) |
| **Транзакционность** | Легко | Сложно (distributed transactions) |

---

## Заключение

### Текущее состояние:

Проект использует **модульную монолитную архитектуру**, которая:
- ✅ Хорошо подходит для текущего размера проекта
- ✅ Упрощает разработку и отладку
- ✅ Обеспечивает низкую латентность
- ❌ НЕ является микросервисной архитектурой

### Рекомендация:

**Остаться на модульной монолитной архитектуре** до тех пор, пока не появятся реальные требования к:
- Независимому масштабированию модулей
- Изоляции отказов между модулями
- Разным командам для разных модулей
- Разным технологическим стекам для разных модулей

Если такие требования появятся, можно мигрировать к микросервисам постепенно, начиная с выделения одного сервиса (например, Ingestion Service).

---

## Примечания

- Архитектура в `docs/architecture.md` упоминает "микросервисная архитектура", но фактически это модульный монолит
- Docker Compose содержит несколько сервисов, но это инфраструктурные сервисы (БД), а не бизнес-логика
- Модульность проекта отличная, что упростит миграцию к микросервисам в будущем, если потребуется

