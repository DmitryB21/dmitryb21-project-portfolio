# Запуск Full Stack (FastAPI + Streamlit UI)

## Обзор

Проект поддерживает одновременный запуск FastAPI сервера и Streamlit UI через единый скрипт.

## Быстрый старт

### Вариант 1: Через скрипт (рекомендуется)

```bash
python scripts/start_full_stack.py
```

### Вариант 2: Через start_project.bat

```bash
scripts\start_project.bat
# Выберите опцию 5: "Запустить Full Stack (FastAPI + Streamlit)"
```

## Что запускается

1. **FastAPI сервер** на `http://localhost:8000` (или 8001, если 8000 занят)
   - API документация: `http://localhost:8000/docs`
   - Health check: `http://localhost:8000/health`

2. **Streamlit UI** на `http://localhost:8501`
   - Чат интерфейс
   - Управление данными Qdrant

## Функционал Streamlit UI

### Вкладка "💬 Чат"

- Задавайте вопросы по документации
- Просматривайте источники ответов
- Отслеживайте метрики качества (Faithfulness, Answer Relevancy, Precision@3)

### Вкладка "⚙️ Управление данными"

#### 🗑️ Удаление векторов

- Удаление коллекции из Qdrant
- Полная очистка всех векторов
- ⚠️ **Внимание:** Действие необратимо!

**Использование:**
1. Введите имя коллекции (по умолчанию: `neuro_docs`)
2. Нажмите "🗑️ Удалить коллекцию"
3. Подтвердите действие

#### 🔄 Запуск индексации

- Запуск индексации документов из `data/NeuroDoc_Data/` в Qdrant
- Процесс выполняется в фоновом режиме
- Проверьте логи FastAPI для отслеживания прогресса

**Использование:**
1. Убедитесь, что документы находятся в `data/NeuroDoc_Data/hr/` и/или `data/NeuroDoc_Data/it/`
2. Нажмите "🚀 Запустить индексацию"
3. Следите за прогрессом в логах FastAPI

#### 📊 Информация о коллекции

- Количество точек в коллекции
- Размерность векторов
- Метрика расстояния

## API Endpoints

### Удаление коллекции

```http
DELETE /admin/qdrant/collection?collection_name=neuro_docs
```

**Ответ:**
```json
{
  "success": true,
  "message": "Коллекция 'neuro_docs' успешно удалена",
  "collection_name": "neuro_docs"
}
```

### Запуск индексации

```http
POST /admin/indexing/start
```

**Ответ:**
```json
{
  "success": true,
  "message": "Индексация запущена в фоновом режиме. Проверьте логи для отслеживания прогресса.",
  "task_id": null
}
```

## Остановка сервисов

Нажмите `Ctrl+C` в терминале, где запущен скрипт. Оба сервиса будут остановлены автоматически.

## Устранение неполадок

### Порт занят

Если порт 8000 занят, скрипт автоматически попробует использовать порт 8001.

### Streamlit не может подключиться к API

Убедитесь, что:
1. FastAPI сервер запущен и доступен
2. Переменная окружения `API_BASE_URL` установлена правильно
3. Нет проблем с файрволом

### Ошибки при индексации

1. Проверьте, что Qdrant запущен
2. Убедитесь, что документы находятся в правильных директориях
3. Проверьте логи FastAPI для деталей ошибки

## Дополнительная информация

- [Архитектура системы](architecture.md)
- [Процесс индексации](indexing_process.md)
- [Быстрый старт](quick_start.md)

