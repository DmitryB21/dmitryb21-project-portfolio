# Neuro_Doc_Assistant - Environment Variables Example
# Скопируйте этот файл в .env и заполните реальными значениями

# GigaChat API - OAuth 2.0 аутентификация
# GIGACHAT_AUTH_KEY - Base64 encoded "Client ID:Client Secret"
# Получить можно в личном кабинете GigaChat: https://developers.sber.ru/portal/products/gigachat
# Формат: base64(Client ID:Client Secret)
GIGACHAT_AUTH_KEY=your_base64_encoded_client_id_client_secret_here

# GIGACHAT_SCOPE - Scope для OAuth 2.0
# Возможные значения:
#   - GIGACHAT_API_PERS (для физических лиц)
#   - GIGACHAT_API_B2B (для ИП и юридических лиц по платным пакетам)
#   - GIGACHAT_API_CORP (для ИП и юридических лиц по схеме pay-as-you-go)
GIGACHAT_SCOPE=GIGACHAT_API_PERS

# GIGACHAT_API_URL - URL для chat completions (обычно не нужно менять)
# GIGACHAT_EMBEDDINGS_URL - URL для embeddings (обычно не нужно менять)
# Используются официальные endpoints по умолчанию:
#   - Chat: https://gigachat.devices.sberbank.ru/api/v1/chat/completions
#   - Embeddings: https://gigachat.devices.sberbank.ru/api/v1/embeddings

# GIGACHAT_MOCK_MODE - Использовать mock mode (для работы без интернета)
# Установите в true, если хотите использовать mock embeddings и ответы
GIGACHAT_MOCK_MODE=false

# RAGAS_MOCK_MODE - Использовать mock mode для RAGAS оценки
# Установите в true, если хотите использовать упрощённую оценку без реальных вызовов LLM
# По умолчанию (false) используется реальный RAGAS с GigaChat LLM и Embeddings
RAGAS_MOCK_MODE=false

# Qdrant
QDRANT_HOST=localhost
QDRANT_PORT=6333
QDRANT_API_KEY=  # Опционально, если используется аутентификация

# PostgreSQL (для будущих модулей)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=neuro_doc_assistant
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password_here

# Embedding Configuration
EMBEDDING_MODEL_VERSION=GigaChat
EMBEDDING_DIM=1536

# Chunking Configuration
# Размер чанка в токенах (рекомендуется: 300-500)
CHUNK_SIZE=400
# Процент overlap между чанками (0.15-0.25, рекомендуется: 0.20)
CHUNK_OVERLAP_PERCENT=0.20

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
API_RELOAD=false

