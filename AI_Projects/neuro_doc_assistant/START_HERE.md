# 🚀 Neuro_Doc_Assistant - Быстрый старт

## ✅ Шаг 1: Проверка установки

Все зависимости уже установлены! Проверка:

```bash
# Активация виртуального окружения
venv\Scripts\activate  # Windows
# или
source venv/bin/activate  # Linux/Mac

# Проверка зависимостей
python -c "import fastapi, uvicorn, streamlit, qdrant_client; print('✅ Все зависимости установлены!')"
```

## 🎯 Шаг 2: Запуск проекта

### Вариант A: Использование скрипта (рекомендуется)

```bash
# Windows:
scripts\start_project.bat

# Linux/Mac:
chmod +x scripts/start_project.sh
./scripts/start_project.sh
```

### Вариант B: Ручной запуск

#### 1. Запуск тестов

```bash
# Все тесты (187 тестов)
python -m pytest tests/ -v

# Или через скрипт
python scripts/run_tests.py all
```

#### 2. Запуск FastAPI сервера

```bash
python app/main.py
```

API будет доступен:
- Основной: http://localhost:8000
- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

#### 3. Запуск Streamlit UI

```bash
streamlit run app/ui/streamlit_app.py
```

UI будет доступен: http://localhost:8501

## 📝 Шаг 3: Тестирование

### Тест через API

```bash
# Проверка здоровья
curl http://localhost:8000/health

# Запрос к агенту
curl -X POST http://localhost:8000/ask \
  -H "Content-Type: application/json" \
  -d "{\"query\": \"Какой SLA у сервиса платежей?\", \"k\": 3}"
```

### Тест через UI

1. Откройте http://localhost:8501
2. Введите вопрос
3. Получите ответ с источниками и метриками

## 🔧 Настройка (опционально)

Для работы с реальным GigaChat API и Qdrant:

1. Отредактируйте `.env` файл
2. Укажите реальные значения:
   - `GIGACHAT_API_KEY` - ваш API ключ
   - `QDRANT_HOST`, `QDRANT_PORT` - параметры Qdrant

**Примечание**: Для тестов используется mock mode, реальные API не требуются.

## 📊 Статистика проекта

- ✅ **187 тестов** (все проходят)
- ✅ **8 модулей** реализованы
- ✅ **6 Use Cases** реализованы
- ✅ **API + UI** готовы к использованию

## 📚 Документация

- `docs/quick_start.md` - подробный quick start guide
- `README.md` - полное описание проекта
- `docs/architecture.md` - архитектура системы

## 🎉 Готово!

Проект готов к использованию. Запустите `scripts\start_project.bat` для интерактивного меню!

