# Neuro Video Copywriter

<div align="center">

**Интеллектуальная платформа для обработки видео, транскрибации и генерации образовательных материалов с использованием AI**

[![Python](https://img.shields.io/badge/Python-3.11+-blue.svg)](https://www.python.org/)
[![FastAPI](https://img.shields.io/badge/FastAPI-0.110+-green.svg)](https://fastapi.tiangolo.com/)
[![React](https://img.shields.io/badge/React-18.2+-61dafb.svg)](https://reactjs.org/)
[![TypeScript](https://img.shields.io/badge/TypeScript-5.3+-3178c6.svg)](https://www.typescriptlang.org/)
[![Docker](https://img.shields.io/badge/Docker-Ready-2496ed.svg)](https://www.docker.com/)

</div>

---

## 📋 Содержание

- [О проекте](#-о-проекте)
- [Основные возможности](#-основные-возможности)
- [Технологический стек](#-технологический-стек)
- [Требования](#-требования)
- [Быстрый старт](#-быстрый-старт)
- [Установка и настройка](#-установка-и-настройка)
- [Использование](#-использование)
- [API документация](#-api-документация)
- [Архитектура](#-архитектура)
- [Разработка](#-разработка)
- [Troubleshooting](#-troubleshooting)
- [Документация](#-документация)
- [Лицензия](#-лицензия)

---

## 🎯 О проекте

**Neuro Video Copywriter** — это полнофункциональное веб-приложение для автоматической обработки образовательных видео с платформ YouTube, VK Video и RuTube. Система извлекает аудио, выполняет транскрибацию, генерирует структурированные методические материалы и предоставляет интеллектуального чат-консультанта на основе RAG (Retrieval-Augmented Generation).

### Целевая аудитория

- **Образовательные платформы**: автоматизация подготовки методических материалов из видеолекций
- **Корпоративные тренинги**: быстрая обработка обучающих видео и создание конспектов
- **Контент-мейкеры**: автоматическая транскрибация и структурирование контента

---

## ✨ Основные возможности

### 🎥 Загрузка и обработка видео

- **Поддержка платформ**: YouTube, VK Video, RuTube
- **Автоопределение провайдера**: автоматическое распознавание источника по URL
- **Извлечение аудио**: автоматическая конвертация в формат, пригодный для транскрибации (WAV, 16 kHz, mono)
- **Нормализация URL**: автоматическая очистка ссылок (удаление префиксов `@`, пробелов)
- **Устойчивость к ошибкам**: расширенные механизмы повторных попыток и обработки сетевых ошибок

### 🎤 Транскрибация

- **Локальный режим**: использование OpenAI Whisper для офлайн-транскрибации
- **Облачный режим**: транскрибация через OpenAI Audio Transcription API
- **Автоматическая нарезка**: обработка больших файлов (>25 MB) с автоматическим разбиением на чанки
- **Поддержка языков**: автоматическое определение языка или явное указание
- **Сохранение истории**: все транскрипты сохраняются в базе данных для повторного использования

### 📚 Генерация методичек

- **Структурированный вывод**: автоматическое создание методических материалов с:
  - Кратким резюме
  - Ключевыми тезисами
  - Важными цитатами с таймкодами
  - Практическими рекомендациями
- **AI-анализ**: использование GPT для глубокого анализа контента
- **Сохранение результатов**: методички сохраняются в БД и доступны для повторного просмотра

### 💬 Чат-консультант

- **RAG-архитектура**: ответы на основе релевантного контекста из транскриптов
- **Векторный поиск**: использование Qdrant для семантического поиска по контенту
- **Контекстные ответы**: консультант учитывает конкретное видео при ответах
- **Источники**: указание фрагментов транскрипта, на основе которых сформирован ответ

### 📊 Управление данными

- **История видео**: просмотр всех загруженных видео с метаданными
- **Артефакты**: управление транскриптами, методичками и аудиофайлами
- **Удаление**: полная очистка данных (видео, транскрипт, методичка, аудио, векторные индексы)

---

## 🛠 Технологический стек

### Backend

- **Framework**: FastAPI 0.110+
- **Language**: Python 3.11+
- **Database**: PostgreSQL 15 (SQLAlchemy 2.0, Alembic)
- **Vector Store**: Qdrant 1.7.3
- **AI/ML**:
  - OpenAI API (GPT-4, Whisper)
  - OpenAI Whisper (локальная транскрибация)
- **Media Processing**:
  - yt-dlp (загрузка видео)
  - FFmpeg (извлечение и обработка аудио)
  - pydub (работа с аудио)
- **Dependencies**: Poetry
- **Code Quality**: ruff, black, isort, mypy

### Frontend

- **Framework**: React 18.2+
- **Language**: TypeScript 5.3+
- **Build Tool**: Vite 5.1+
- **Styling**: Tailwind CSS 3.4+
- **State Management**: Zustand 4.5+
- **Data Fetching**: React Query (TanStack Query) 5.24+
- **HTTP Client**: Axios 1.6+
- **UI Components**: shadcn-ui
- **Package Manager**: pnpm

### Infrastructure

- **Containerization**: Docker, Docker Compose
- **Networking**: Bridge network для межсервисного взаимодействия
- **Volumes**: Персистентное хранение данных (PostgreSQL, Qdrant, медиафайлы)

---

## 📦 Требования

### Системные требования

- **Docker**: 20.10+
- **Docker Compose**: 2.0+
- **RAM**: минимум 4 GB (рекомендуется 8+ GB для локальной транскрибации)
- **Disk Space**: минимум 10 GB свободного места
- **OS**: Linux, macOS, Windows (с WSL2)

### Внешние сервисы

- **OpenAI API Key**: обязателен для API-транскрибации и генерации методичек
- **Интернет-соединение**: для загрузки видео и работы с OpenAI API

---

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd neuro_video_copywriter
```

### 2. Настройка окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Отредактируйте `.env` и укажите обязательные параметры:

```env
# Обязательно: OpenAI API ключ
NVC_OPENAI_API_KEY=your_openai_api_key_here

# База данных (для Docker используйте postgres вместо localhost)
NVC_DATABASE_URL=postgresql+psycopg://nvc_user:nvc_password@postgres:5432/nvc_db

# Qdrant (для Docker используйте qdrant вместо localhost)
NVC_QDRANT_URL=http://qdrant:6333
```

### 3. Запуск приложения

#### Быстрая сборка (рекомендуется, ~5-10 минут)

Без локального Whisper, использует только API-транскрибацию:

```bash
docker compose -f docker-compose.yml -f docker-compose.fast.yml up --build
```

#### Полная сборка (с локальной транскрибацией, ~1-2 часа)

Включает установку OpenAI Whisper для офлайн-транскрибации:

```bash
docker compose up --build
```

#### Запуск в фоновом режиме

```bash
docker compose up -d
```

### 4. Проверка статуса

```bash
docker compose ps
```

Все сервисы должны быть в статусе `Up`:
- `nvc-backend` (порт 8000)
- `nvc-frontend` (порт 5173)
- `nvc-postgres` (порт 5432)
- `nvc-qdrant` (порты 6333, 6334)

### 5. Открытие приложения

- **Frontend**: http://localhost:5173
- **Backend API**: http://localhost:8000
- **API Documentation**: http://localhost:8000/docs
- **Qdrant Dashboard**: http://localhost:6333/dashboard

---

## ⚙️ Установка и настройка

### Конфигурация переменных окружения

Полный список переменных доступен в `.env.example`. Основные параметры:

| Переменная | Описание | Обязательно | По умолчанию |
|------------|----------|--------------|--------------|
| `NVC_OPENAI_API_KEY` | API ключ OpenAI | ✅ Да | - |
| `NVC_DATABASE_URL` | Строка подключения к PostgreSQL | ✅ Да | - |
| `NVC_QDRANT_URL` | URL Qdrant сервера | ✅ Да | `http://localhost:6333` |
| `NVC_QDRANT_API_KEY` | API ключ Qdrant (опционально) | ❌ Нет | - |
| `NVC_WHISPER_MODEL` | Модель Whisper для локальной транскрибации | ❌ Нет | `base` |
| `NVC_ENVIRONMENT` | Окружение (development/production) | ❌ Нет | `development` |
| `NVC_LOG_LEVEL` | Уровень логирования | ❌ Нет | `INFO` |

### Оптимизация сборки

Для ускорения разработки используйте быстрый профиль сборки (без Whisper):

```bash
# Использование docker-compose.fast.yml
docker compose -f docker-compose.yml -f docker-compose.fast.yml up --build
```

Подробнее см. [docs/BUILD_OPTIMIZATION.md](docs/BUILD_OPTIMIZATION.md)

### Первоначальная настройка базы данных

Миграции Alembic применяются автоматически при первом запуске. Для ручного применения:

```bash
docker compose exec backend alembic upgrade head
```

---

## 📖 Использование

### Рабочий процесс

1. **Загрузка видео**
   - Откройте http://localhost:5173
   - Введите URL видео (YouTube, VK, RuTube)
   - Выберите провайдера или оставьте "Автоопределение"
   - Нажмите "Загрузить и извлечь аудио"

2. **Транскрибация**
   - После загрузки выберите режим транскрибации:
     - **Локальный (Whisper)**: офлайн-обработка, требует установки Whisper
     - **Онлайн (OpenAI API)**: быстрая обработка через API, автоматическая нарезка больших файлов
   - Дождитесь завершения транскрибации

3. **Генерация методички**
   - После транскрибации нажмите "Сгенерировать методичку"
   - Система создаст структурированный документ с ключевыми тезисами и цитатами

4. **Чат-консультант**
   - Задайте вопрос о содержании видео
   - Консультант ответит на основе релевантных фрагментов транскрипта
   - Просмотрите источники ответа

### История и управление

- **Просмотр истории**: список всех загруженных видео доступен в разделе "История"
- **Повторное использование**: выберите видео из истории для повторной транскрибации или генерации методички
- **Удаление**: удалите видео вместе со всеми связанными данными (транскрипт, методичка, аудио, векторные индексы)

---

## 🔌 API документация

### Основные эндпоинты

#### `POST /api/video/extract`

Загрузка видео и извлечение аудио.

**Request:**
```json
{
  "video_url": "https://www.youtube.com/watch?v=...",
  "provider": "auto",
  "request_id": "optional-uuid",
  "metadata": {}
}
```

**Response:**
```json
{
  "video_id": "uuid",
  "audio_path": "/data/audio/...",
  "provider": "youtube",
  "metadata": {
    "title": "Video Title",
    "uploader": "Channel Name",
    "duration": 1234
  }
}
```

#### `GET /api/videos`

Список загруженных видео.

**Response:**
```json
{
  "items": [
    {
      "id": "uuid",
      "title": "Video Title",
      "source_url": "https://...",
      "provider": "youtube",
      "status": "downloaded",
      "audio_path": "...",
      "created_at": "2025-11-18T10:15:00Z"
    }
  ]
}
```

#### `GET /api/videos/{video_id}`

Детальная информация о видео с артефактами.

#### `DELETE /api/videos/{video_id}`

Удаление видео и всех связанных данных.

#### `POST /api/transcribe/local`

Локальная транскрибация через Whisper.

**Request:**
```json
{
  "video_id": "uuid",
  "audio_path": "/data/audio/...",
  "options": {
    "model": "base",
    "language": "ru"
  }
}
```

#### `POST /api/transcribe/online`

Транскрибация через OpenAI API.

**Request:**
```json
{
  "video_id": "uuid",
  "audio_path": "/data/audio/...",
  "api_options": {
    "model": "whisper-1",
    "language": "ru"
  }
}
```

#### `POST /api/summary`

Генерация методички.

**Request:**
```json
{
  "video_id": "uuid",
  "transcript_id": "uuid",
  "options": {
    "style": "academic",
    "language": "ru"
  }
}
```

#### `POST /api/chat`

Чат-консультант с контекстом.

**Request:**
```json
{
  "video_id": "uuid",
  "question": "Что такое машинное обучение?",
  "top_k": 5,
  "language": "ru"
}
```

**Response:**
```json
{
  "answer": "Машинное обучение — это...",
  "sources": [
    {
      "chunk_text": "...",
      "metadata": {
        "video_id": "uuid",
        "chunk_index": 0
      }
    }
  ],
  "model": "gpt-4"
}
```

### Интерактивная документация

Полная документация API доступна по адресу:
- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc

---

## 🏗 Архитектура

### Высокоуровневая схема

```
┌─────────────┐
│   Browser   │
└──────┬──────┘
       │ HTTP
       ▼
┌─────────────────┐
│  Frontend       │
│  (React + Vite)  │
└──────┬──────────┘
       │ REST API
       ▼
┌─────────────────┐
│   Backend       │
│   (FastAPI)     │
└──┬──┬──┬──┬──┬──┘
   │  │  │  │  │
   │  │  │  │  └──► Qdrant (Vector Store)
   │  │  │  │
   │  │  │  └──► PostgreSQL (Metadata)
   │  │  │
   │  │  └──► OpenAI API (GPT, Whisper)
   │  │
   │  └──► yt-dlp (Video Download)
   │
   └──► FFmpeg (Audio Extraction)
```

### Компоненты Backend

#### Сервисы

- **`VideoDownloadService`**: Оркестрация загрузки видео через адаптеры провайдеров
- **`AudioExtractor`**: Извлечение аудио из видео через FFmpeg
- **`LocalTranscriptionService`**: Локальная транскрибация через Whisper
- **`APITranscriptionService`**: Транскрибация через OpenAI API с автоматической нарезкой
- **`AudioChunker`**: Разбиение больших аудиофайлов на чанки
- **`SummaryGenerator`**: Генерация структурированных методичек
- **`ConsultantAgent`**: RAG-агент для ответов на вопросы
- **`EmbeddingService`**: Генерация векторных представлений текста
- **`TranscriptIndexer`**: Индексация транскриптов в Qdrant
- **`VectorStoreClient`**: Клиент для работы с Qdrant

#### Адаптеры провайдеров

- **`YouTubeAdapter`**: Загрузка с YouTube (yt-dlp с расширенными заголовками и ffmpeg)
- **`VkAdapter`**: Загрузка с VK Video (поддержка vk.com и vkvideo.ru)
- **`RuTubeAdapter`**: Загрузка с RuTube

#### Репозитории

- **`VideoRepository`**: CRUD операции для видео
- **`TranscriptRepository`**: Управление транскриптами
- **`SummaryRepository`**: Управление методичками
- **`ProcessingJobRepository`**: Мониторинг фоновых задач

### Компоненты Frontend

- **`VideoInput`**: Форма загрузки видео с выбором провайдера
- **`VideoHistory`**: Список загруженных видео
- **`TranscriptView`**: Отображение транскрипта
- **`SummaryCard`**: Отображение методички
- **`ChatAssistant`**: Интерфейс чат-консультанта
- **`ProcessingStatus`**: Индикатор статуса обработки

### Поток данных

1. Пользователь вводит URL → Frontend отправляет запрос в Backend
2. Backend определяет провайдера → Загружает видео через адаптер
3. Извлекается аудио → Сохраняется в хранилище
4. Аудио транскрибируется → Текст сохраняется в PostgreSQL
5. Транскрипт индексируется → Чанки с эмбеддингами сохраняются в Qdrant
6. Генерируется методичка → Сохраняется в PostgreSQL
7. Пользователь задаёт вопрос → RAG-агент находит релевантные фрагменты → Формирует ответ

Подробнее см. [docs/Project.md](docs/Project.md)

---

## 💻 Разработка

### Структура проекта

```
neuro_video_copywriter/
├── backend/              # Backend приложение (FastAPI)
│   ├── app.py           # Точка входа FastAPI
│   ├── config.py         # Конфигурация (Pydantic Settings)
│   ├── database/         # SQLAlchemy модели и сессии
│   ├── models/           # Pydantic схемы
│   ├── repositories/    # Репозитории для работы с БД
│   ├── services/         # Бизнес-логика
│   │   ├── providers/   # Адаптеры провайдеров
│   │   ├── storage/     # Хранилище медиа
│   │   └── vector_store/# Клиент Qdrant
│   ├── migrations/       # Alembic миграции
│   └── tests/           # Тесты
├── frontend/            # Frontend приложение (React)
│   ├── src/
│   │   ├── components/  # React компоненты
│   │   ├── services/    # API клиенты
│   │   └── store/       # Zustand store
│   └── package.json
├── docs/                # Документация
│   ├── Project.md      # Архитектура проекта
│   ├── changelog.md    # История изменений
│   └── ...
├── docker-compose.yml   # Docker Compose конфигурация
└── .env.example         # Пример переменных окружения
```

### Локальная разработка

#### Backend

```bash
cd backend

# Установка зависимостей
poetry install

# Активация виртуального окружения
poetry shell

# Запуск сервера разработки
uvicorn app:app --reload --host 0.0.0.0 --port 8000
```

#### Frontend

```bash
cd frontend

# Установка зависимостей
pnpm install

# Запуск dev-сервера
pnpm dev
```

### Code Style

#### Backend (Python)

```bash
# Форматирование
black backend/
isort backend/

# Линтинг
ruff check backend/
mypy backend/
```

#### Frontend (TypeScript)

```bash
# Линтинг и форматирование (если настроено)
pnpm lint
pnpm format
```

### Тестирование

```bash
# Backend тесты
cd backend
pytest

# Frontend тесты (если настроено)
cd frontend
pnpm test
```

### Миграции базы данных

```bash
# Создание новой миграции
docker compose exec backend alembic revision --autogenerate -m "Description"

# Применение миграций
docker compose exec backend alembic upgrade head

# Откат миграции
docker compose exec backend alembic downgrade -1
```

### Пересборка контейнеров

```bash
# Пересборка конкретного сервиса
docker compose build backend
docker compose build frontend

# Пересборка без кеша
docker compose build --no-cache backend

# Перезапуск сервиса
docker compose restart backend
```

---

## 🔧 Troubleshooting

### Backend не запускается

**Проблема**: Ошибки при запуске backend контейнера

**Решения**:
1. Проверьте логи:
   ```bash
   docker compose logs backend
   ```

2. Убедитесь, что в `.env` указан `NVC_OPENAI_API_KEY`:
   ```bash
   grep NVC_OPENAI_API_KEY .env
   ```

3. Проверьте подключение к PostgreSQL и Qdrant:
   ```bash
   docker compose ps
   ```

4. Пересоберите образ:
   ```bash
   docker compose build --no-cache backend
   docker compose up backend
   ```

### Frontend не подключается к Backend

**Проблема**: CORS ошибки или `ERR_CONNECTION_REFUSED`

**Решения**:
1. Проверьте, что backend запущен:
   ```bash
   docker compose ps backend
   ```

2. Проверьте CORS настройки в `backend/config.py`:
   ```python
   cors_allow_origins = ["http://localhost:5173"]
   ```

3. Проверьте логи backend на наличие ошибок:
   ```bash
   docker compose logs backend | tail -50
   ```

### Ошибки при загрузке видео

**Проблема**: `HTTP Error 403: Forbidden` при загрузке с YouTube

**Решения**:
1. YouTube может блокировать запросы. Система использует расширенные заголовки и ffmpeg для обхода блокировок.
2. Попробуйте другой URL или подождите несколько минут.
3. Проверьте логи:
   ```bash
   docker compose logs backend | grep -i "403\|error"
   ```

### Транскрибация не завершается

**Проблема**: Таймаут при транскрибации больших файлов

**Решения**:
1. Для больших файлов (>25 MB) используется автоматическая нарезка на чанки.
2. Убедитесь, что таймаут фронтенда достаточен (20 минут по умолчанию).
3. Для локальной транскрибации используйте меньшую модель Whisper (`base` вместо `large`).

### Чат-консультант не находит контекст

**Проблема**: Ответ "No relevant context found"

**Решения**:
1. Убедитесь, что транскрипт был успешно проиндексирован в Qdrant.
2. Проверьте наличие данных в Qdrant:
   ```bash
   docker compose exec backend python -c "from services.vector_store.client import VectorStoreClient, COLLECTION_NAME; from config import get_settings; client=VectorStoreClient(get_settings()); points=client._client.scroll(collection_name=COLLECTION_NAME, limit=5); print(f'Points: {len(points[0])}')"
   ```

3. Переиндексируйте транскрипты (если необходимо):
   ```bash
   docker compose exec backend python -c "
   from services.transcript_indexer import TranscriptIndexer
   from repositories.transcript_repository import TranscriptRepository
   from database.session import get_session_factory
   from config import get_settings
   # ... код переиндексации
   "
   ```

### Проблемы с базой данных

**Проблема**: Ошибки подключения к PostgreSQL

**Решения**:
1. Проверьте статус контейнера:
   ```bash
   docker compose ps postgres
   ```

2. Проверьте логи:
   ```bash
   docker compose logs postgres
   ```

3. Примените миграции вручную:
   ```bash
   docker compose exec backend alembic upgrade head
   ```

### Полная переустановка

Если ничего не помогает, выполните полную переустановку:

```bash
# Остановка и удаление контейнеров
docker compose down -v

# Удаление образов
docker compose rm -f

# Пересборка
docker compose build --no-cache

# Запуск
docker compose up
```

---

## 📚 Документация

Дополнительная документация доступна в директории `docs/`:

- **[QUICKSTART.md](docs/QUICKSTART.md)** — Быстрый старт
- **[SETUP.md](docs/SETUP.md)** — Детальная настройка
- **[Project.md](docs/Project.md)** — Архитектура и спецификация проекта
- **[BUILD_OPTIMIZATION.md](docs/BUILD_OPTIMIZATION.md)** — Оптимизация сборки Docker образов
- **[changelog.md](docs/changelog.md)** — История изменений
- **[Tasktracker.md](docs/Tasktracker.md)** — Отслеживание задач

---

## 📝 Лицензия

Этот проект разработан в образовательных целях и для демонстрации компетенций в области fullstack-разработки и интеграции AI-сервисов.

---

## 🤝 Вклад в проект

Проект находится в активной разработке. Предложения по улучшению и сообщения об ошибках приветствуются.

---

## 📧 Контакты

Для вопросов и предложений создайте issue в репозитории проекта.

---

<div align="center">

**Сделано с ❤️ для автоматизации образовательных процессов**

</div>
