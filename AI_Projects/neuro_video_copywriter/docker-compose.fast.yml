# Быстрая сборка без Whisper (для разработки)
# Использование: docker compose -f docker-compose.yml -f docker-compose.fast.yml up --build

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.fast
    # Остальные настройки наследуются из docker-compose.yml

