# Neuro Doc Data Generator

<div align="center">

**Генератор корпоративной документации через GigaChat API для создания структурированных HR, IT и onboarding материалов**

[![Python](https://img.shields.io/badge/Python-3.11+-blue.svg)](https://www.python.org/)
[![Jupyter](https://img.shields.io/badge/Jupyter-Notebook-orange.svg)](https://jupyter.org/)
[![GigaChat](https://img.shields.io/badge/GigaChat-API-green.svg)](https://developers.sber.ru/gigachat)
[![Google Colab](https://img.shields.io/badge/Google-Colab-F9AB00.svg)](https://colab.research.google.com/)
[![License](https://img.shields.io/badge/License-CC--BY--NC--4.0-lightgrey.svg)](https://creativecommons.org/licenses/by-nc/4.0/)

</div>

---

## 📋 Содержание

- [О проекте](#-о-проекте)
- [Основные возможности](#-основные-возможности)
- [Технологический стек](#-технологический-стек)
- [Требования](#-требования)
- [Быстрый старт](#-быстрый-старт)
- [Установка и настройка](#-установка-и-настройка)
- [Использование](#-использование)
- [Архитектура](#-архитектура)
- [API документация](#-api-документация)
- [Разработка](#-разработка)
- [Troubleshooting](#-troubleshooting)
- [Результаты](#-результаты)
- [Лицензия](#-лицензия)

---

## 🎯 О проекте

**Neuro Doc Data Generator** — это автоматизированная система для генерации корпоративной документации с использованием российской нейросетевой модели GigaChat. Проект создаёт структурированные документы различных категорий (HR, IT, onboarding, compliance) для последующего использования в RAG-системах, корпоративных базах знаний и образовательных платформах.

Проект реализован в виде Jupyter Notebook для удобного запуска в Google Colab, что позволяет использовать его без локальной установки зависимостей и с бесплатными вычислительными ресурсами.

### Целевая аудитория

- **Корпорации**: автоматизация создания внутренней документации (HR-политики, IT-инструкции)
- **Образовательные платформы**: генерация обучающих материалов и гайдов
- **RAG-системы**: создание датасетов для обучения поисковых ассистентов
- **Контент-мейкеры**: быстрая генерация структурированных документов

### Уникальные особенности

- ✅ **Российская LLM**: использование GigaChat API для генерации русскоязычного контента
- ✅ **Клауд-решение**: полная работа в Google Colab без локальной установки
- ✅ **Автоматизация**: генерация десятков документов с минимальным участием пользователя
- ✅ **Структурированный вывод**: создание документов в формате Markdown для удобной интеграции

---

## ✨ Основные возможности

### 📄 Генерация HR-документов

- **Автоматическое создание** политик и процедур компании
- **Темы включают**: удалённая работа, отпуска, больничные, корпоративная этика, KPI, увольнения
- **Объём**: до 50 уникальных документов за сессию
- **Формат**: структурированные Markdown-документы с заголовками и разделами

### 💻 Генерация IT-инструкций

- **Техническая документация**: пошаговые инструкции и руководства
- **Темы включают**: VPN, базы данных, корпоративная почта, BYOD, пароли, Jira/Confluence
- **Объём**: до 50 уникальных инструкций за сессию
- **Стиль**: технический, пошаговый, с нумерованными шагами

### 🆕 Онбординг-гайды

- **Ориентационные материалы** для новых сотрудников
- **Темы включают**: первый день, корпоративные инструменты, безопасность, контакты
- **Стиль**: дружелюбный, информативный, с чек-листами
- **Объём**: 5 базовых гайдов

### ⚖️ Compliance документы

- **Автоматическое скачивание** законов РФ (Трудовой кодекс, Гражданский кодекс, Уголовный кодекс)
- **Конвертация**: PDF → DOCX → Markdown
- **Структурирование**: автоматическое форматирование статей

### 💾 Интеграция с Google Drive

- **Автоматическое сохранение** всех сгенерированных документов
- **Организация**: структурированное хранение по категориям
- **Доступность**: документы доступны из любого места через Google Drive

---

## 🛠 Технологический стек

### Core

- **Language**: Python 3.11+
- **Notebook Environment**: Jupyter Notebook / Google Colab
- **Async Framework**: aiohttp 3.9+
- **HTTP Client**: requests 2.31+

### AI & ML

- **LLM Provider**: GigaChat API
  - OAuth 2.0 авторизация
  - Модель: GigaChat / GigaChat-Pro
  - Streaming: поддерживается
- **Embeddings**: через GigaChat API (опционально)

### Media Processing

- **PDF Processing**: pdf2docx
- **Document Conversion**: pandoc
- **File Operations**: pathlib, shutil

### Cloud Services

- **Notebook Platform**: Google Colab
- **Storage**: Google Drive (автоматическая интеграция)
- **Secrets Management**: Colab Secrets API

### Utilities

- **UUID Generation**: uuid (для RqUID)
- **Async Processing**: asyncio
- **Date/Time**: datetime

---

## 📦 Требования

### Системные требования

- **Платформа**: Google Colab (рекомендуется) или локальный Jupyter
- **RAM**: минимум 2 GB (для Google Colab бесплатно предоставляется)
- **Disk Space**: ~500 MB для временных файлов
- **Internet**: стабильное соединение для работы с GigaChat API

### Внешние сервисы

- **GigaChat API**: требуется регистрация и получение ключей доступа
  - Client ID и Client Secret
  - Authorization Key (Base64 от Client ID:Client Secret)
  - Scope: GIGACHAT_API_PERS, GIGACHAT_API_B2B или GIGACHAT_API_CORP
- **Google Account**: для использования Google Colab и Google Drive

### API Limits

- **Rate Limiting**: до 10 запросов на получение токена в секунду
- **Token Expiry**: токен доступа действителен 30 минут
- **Request Size**: до 2000 токенов на запрос генерации

---

## 🚀 Быстрый старт

### 1. Открытие в Google Colab

1. Загрузите файл `neuro_doc_data_generator.ipynb` в Google Colab
2. Откройте файл через Google Colab: `File → Open Notebook → Upload`

### 2. Настройка секретов

Перейдите в `Runtime → Secrets` и добавьте:

```
GIGACHAT_AUTH_KEY: <ваш_authorization_key>
GIGACHAT_SCOPE: GIGACHAT_API_PERS (или другой scope)
```

**Как получить Authorization Key:**

1. Зарегистрируйтесь на [GigaChat для разработчиков](https://developers.sber.ru/gigachat)
2. Получите Client ID и Client Secret в личном кабинете
3. Закодируйте в Base64: `base64(Client ID:Client Secret)`
4. Добавьте результат в секреты Colab

**Пример:**
```python
import base64
client_id = "your_client_id"
client_secret = "your_client_secret"
auth_key = base64.b64encode(f"{client_id}:{client_secret}".encode()).decode()
print(auth_key)  # Используйте это значение в секретах
```

### 3. Запуск генерации

1. Подключите Google Drive (ячейка автоматически запросит авторизацию)
2. Запустите все ячейки последовательно: `Runtime → Run All`
3. Дождитесь завершения генерации (может занять 30-60 минут для полного набора)

### 4. Результаты

Все документы будут сохранены:
- **Локально в Colab**: `/content/neurodoc_data/`
- **В Google Drive**: `/content/drive/MyDrive/NeuroDoc_Data/`

Структура:
```
NeuroDoc_Data/
├── hr/              # HR-документы (50 шт)
├── it/              # IT-инструкции (50 шт)
├── onboarding/      # Онбординг-гайды (5 шт)
└── compliance/      # Законы РФ (3 шт: ТК, ГК, УК)
```

---

## ⚙️ Установка и настройка

### Локальная установка (альтернатива Colab)

Если вы хотите запускать проект локально:

```bash
# Клонирование репозитория
git clone <repository-url>
cd neuro_doc_data_generator

# Создание виртуального окружения
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установка зависимостей
pip install aiohttp requests pdf2docx pandoc pathlib

# Установка Jupyter
pip install jupyter notebook

# Запуск Jupyter
jupyter notebook neuro_doc_data_generator.ipynb
```

### Настройка переменных окружения

Для локального запуска создайте файл `.env`:

```env
GIGACHAT_AUTH_KEY=your_base64_encoded_key
GIGACHAT_SCOPE=GIGACHAT_API_PERS
```

И обновите код в ноутбуке для чтения из `.env`:

```python
from dotenv import load_dotenv
import os

load_dotenv()
GIGACHAT_AUTH_KEY = os.getenv("GIGACHAT_AUTH_KEY")
GIGACHAT_SCOPE = os.getenv("GIGACHAT_SCOPE", "GIGACHAT_API_PERS")
```

### Настройка Google Drive (для локального запуска)

Для локального запуска нужно настроить авторизацию Google Drive API:

```bash
pip install google-auth google-auth-oauthlib google-auth-httplib2 google-api-python-client
```

И получить OAuth credentials с [Google Cloud Console](https://console.cloud.google.com/).

---

## 📖 Использование

### Рабочий процесс

1. **Подготовка**
   - Откройте ноутбук в Google Colab
   - Добавьте секреты GigaChat API
   - Подключите Google Drive

2. **Скачивание законов** (ячейка 3)
   - Автоматическое скачивание PDF законов РФ
   - Конвертация в Markdown
   - Сохранение в `compliance/`

3. **Генерация HR-документов** (ячейка 5)
   - Генерация 50 HR-документов
   - Темы: политики, процедуры, регламенты
   - Сохранение в `hr/`

4. **Генерация IT-инструкций** (ячейка 5)
   - Генерация 50 IT-инструкций
   - Темы: технические руководства, инструкции
   - Сохранение в `it/`

5. **Генерация онбординг-гайдов** (ячейка 5)
   - Генерация 5 онбординг-документов
   - Темы: введение в компанию, инструменты
   - Сохранение в `onboarding/`

6. **Сохранение на Google Drive** (ячейка 6)
   - Автоматическое копирование всех файлов
   - Доступ из любого места

### Кастомизация промптов

Вы можете изменить промпты для генерации документов в ячейке 4:

```python
HR_PROMPT = """Напиши официальный HR-документ на тему: «{topic}».
Стиль: деловой, структурированный, с заголовками.
Объём: 300–500 слов.
Пиши на русском языке.
Формат: только Markdown без кодовых блоков."""

IT_PROMPT = """Напиши официальную IT-инструкцию на тему: «{topic}».
Стиль: пошаговый, технический, с нумерованными шагами.
Объём: 300–500 слов.
Пиши на русском языке.
Формат: только Markdown без кодовых блоков."""
```

### Добавление новых тем

Расширьте списки тем в ячейке 4:

```python
HR_TOPICS = [
    "Новая тема 1",
    "Новая тема 2",
    # ... добавьте свои темы
] * 5  # Умножьте на нужное количество документов
```

### Изменение параметров генерации

Настройте параметры модели в функции `generate_with_gigachat`:

```python
content = await generate_with_gigachat(
    session, 
    prompt, 
    model="GigaChat-Pro",  # Или "GigaChat"
    temperature=0.7,        # 0.0-2.0 (креативность)
    max_tokens=2000        # Максимум токенов в ответе
)
```

---

## 🏗 Архитектура

### Высокоуровневая схема

```
┌─────────────────┐
│  Google Colab   │
│  (Jupyter Env)  │
└────────┬────────┘
         │
         ├──► GigaChat OAuth API (токен)
         │    └─► https://ngw.devices.sberbank.ru:9443/api/v2/oauth
         │
         ├──► GigaChat API (генерация)
         │    └─► https://gigachat.devices.sberbank.ru/api/v1/chat/completions
         │
         ├──► Законы РФ (скачивание)
         │    └─► Garant.ru, Fintech.ru
         │
         └──► Google Drive (сохранение)
              └─► /content/drive/MyDrive/NeuroDoc_Data/
```

### Компоненты системы

#### 1. Модуль авторизации

- **`get_gigachat_token()`**: получение OAuth токена
  - Генерация RqUID (UUID4)
  - Отправка запроса с Basic авторизацией
  - Кэширование токена (действителен 30 минут)
  - Обработка SSL-сертификатов (отключена проверка для OAuth endpoint)

#### 2. Модуль генерации

- **`generate_with_gigachat()`**: генерация текста через API
  - Формирование запроса в формате Chat Completion API
  - Обработка ответа и извлечение контента
  - Статистика использования токенов
  - Детальное логирование

#### 3. Модуль обработки документов

- **`generate_and_save()`**: оркестрация генерации и сохранения
  - Форматирование промптов
  - Генерация безопасных имён файлов
  - Сохранение в Markdown формате

#### 4. Модуль загрузки законов

- **`download_file()`**: скачивание PDF
- **`pdf_to_md()`**: конвертация PDF → DOCX → Markdown
- Обработка структуры документов

### Поток данных

```
1. Инициализация
   ├──► Загрузка секретов из Colab Secrets
   ├──► Подключение Google Drive
   └──► Создание директорий

2. Скачивание законов
   ├──► Запрос PDF с источников
   ├──► Конвертация в Markdown
   └──► Сохранение в compliance/

3. Генерация документов (для каждой категории)
   ├──► Получение токена (кэшируется)
   ├──► Формирование промпта
   ├──► Запрос к GigaChat API
   ├──► Извлечение контента
   └──► Сохранение в Markdown

4. Сохранение результатов
   └──► Копирование в Google Drive
```

### Обработка ошибок

- **SSL-сертификаты**: автоматическое отключение проверки для OAuth endpoint
- **Таймауты**: настройка timeout для долгих запросов (60 секунд)
- **Повторные попытки**: кэширование токена для избежания лишних запросов
- **Логирование**: детальная информация о каждом шаге для отладки

---

## 🔌 API документация

### GigaChat API Endpoints

#### Получение токена доступа

**Endpoint**: `POST https://ngw.devices.sberbank.ru:9443/api/v2/oauth`

**Headers:**
```
Content-Type: application/x-www-form-urlencoded
Accept: application/json
RqUID: <uuid4>
Authorization: Basic <base64_encoded_client_id:client_secret>
```

**Body:**
```
scope=GIGACHAT_API_PERS
```

**Response:**
```json
{
  "access_token": "eyJ...",
  "expires_at": 1739784663483
}
```

#### Генерация текста

**Endpoint**: `POST https://gigachat.devices.sberbank.ru/api/v1/chat/completions`

**Headers:**
```
Authorization: Bearer <access_token>
Content-Type: application/json
X-Request-ID: <uuid4> (опционально)
```

**Body:**
```json
{
  "model": "GigaChat",
  "messages": [
    {
      "role": "user",
      "content": "Ваш промпт здесь"
    }
  ],
  "temperature": 0.7,
  "max_tokens": 2000,
  "stream": false
}
```

**Response:**
```json
{
  "choices": [
    {
      "message": {
        "content": "Сгенерированный текст...",
        "role": "assistant"
      },
      "index": 0,
      "finish_reason": "stop"
    }
  ],
  "created": 1760434636,
  "model": "GigaChat:2.0.28.2",
  "object": "chat.completion",
  "usage": {
    "prompt_tokens": 55,
    "completion_tokens": 30,
    "total_tokens": 85,
    "precached_prompt_tokens": 4
  }
}
```

Подробная документация доступна в файле `GigaChat API.txt`.

---

## 💻 Разработка

### Структура проекта

```
neuro_doc_data_generator/
├── neuro_doc_data_generator.ipynb  # Основной ноутбук
├── GigaChat API.txt                # Документация API
├── README.md                       # Этот файл
└── data/                          # Сгенерированные данные (опционально)
    ├── hr/
    ├── it/
    ├── onboarding/
    └── compliance/
```

### Ключевые функции

#### `get_gigachat_token(session=None)`

Получает токен доступа GigaChat API с кэшированием.

**Параметры:**
- `session`: aiohttp.ClientSession (не используется, оставлен для совместимости)

**Возвращает:**
- `str`: access_token или `None` при ошибке

**Особенности:**
- Кэширование токена на 30 минут
- Автоматическое обновление за минуту до истечения
- Детальное логирование процесса

#### `generate_with_gigachat(session, prompt, model="GigaChat", temperature=0.7, max_tokens=2000)`

Генерирует текст через GigaChat API.

**Параметры:**
- `session`: aiohttp.ClientSession (не используется)
- `prompt`: str — промпт для генерации
- `model`: str — модель GigaChat ("GigaChat" или "GigaChat-Pro")
- `temperature`: float — температура генерации (0.0-2.0)
- `max_tokens`: int — максимум токенов в ответе

**Возвращает:**
- `str`: сгенерированный текст или пустая строка при ошибке

**Особенности:**
- Автоматическое получение токена
- Статистика использования токенов
- Обработка ошибок с детальным логированием

#### `generate_and_save(session, topic, index, category, prompt_template, model="GigaChat")`

Генерирует документ и сохраняет его в файл.

**Параметры:**
- `session`: aiohttp.ClientSession
- `topic`: str — тема документа
- `index`: int — индекс документа
- `category`: str — категория ("hr", "it", "onboarding")
- `prompt_template`: str — шаблон промпта с `{topic}`
- `model`: str — модель GigaChat

**Возвращает:**
- `bool`: True при успехе, False при ошибке

### Расширение функциональности

#### Добавление новых категорий документов

1. Добавьте список тем:
```python
NEW_CATEGORY_TOPICS = [
    "Тема 1",
    "Тема 2",
]
```

2. Создайте промпт:
```python
NEW_CATEGORY_PROMPT = """Напиши документ на тему: «{topic}».
Стиль: ваш стиль.
Объём: ваш объём.
Пиши на русском языке.
Формат: только Markdown."""
```

3. Добавьте генерацию:
```python
async with aiohttp.ClientSession() as session:
    for i, topic in enumerate(NEW_CATEGORY_TOPICS, 1):
        await generate_and_save(
            session, topic, i-1, "new_category", 
            NEW_CATEGORY_PROMPT, model="GigaChat"
        )
```

#### Использование других моделей

GigaChat API поддерживает несколько моделей:
- `GigaChat` — базовая модель
- `GigaChat-Pro` — продвинутая модель
- `GigaChat-2-Pro` — последняя версия (может требовать специальных прав)

Измените параметр `model` в вызовах `generate_with_gigachat()`.

---

## 🔧 Troubleshooting

### Ошибка: "get() takes 1 positional argument but 2 were given"

**Проблема**: `userdata.get()` в Colab не поддерживает значение по умолчанию.

**Решение**: Код уже исправлен. Убедитесь, что используете последнюю версию ноутбука.

### Ошибка: "SSL: CERTIFICATE_VERIFY_FAILED"

**Проблема**: Проблема с SSL-сертификатами на OAuth и API endpoints.

**Решение**: Код автоматически отключает проверку SSL для этих endpoints. Если ошибка сохраняется, убедитесь, что используете последнюю версию ноутбука.

### Ошибка: "401 Unauthorized"

**Проблема**: Неверный Authorization Key или истёк токен.

**Решения**:
1. Проверьте, что `GIGACHAT_AUTH_KEY` правильно закодирован в Base64
2. Убедитесь, что Scope соответствует вашему типу аккаунта
3. Проверьте логи получения токена в выводе ноутбука

### Ошибка: "403 Forbidden"

**Проблема**: Недостаточно прав или неверный Scope.

**Решения**:
1. Проверьте тип вашего аккаунта GigaChat (физическое лицо, ИП, юридическое лицо)
2. Убедитесь, что Scope соответствует: `GIGACHAT_API_PERS` для физ. лиц
3. Проверьте лимиты API в личном кабинете GigaChat

### Токен не кэшируется

**Проблема**: Токен запрашивается при каждом вызове.

**Решение**: Проверьте логи — должно быть сообщение "♻️ Используем кэшированный токен". Если нет, убедитесь, что глобальные переменные `_access_token_cache` и `_token_expires_at` не сбрасываются.

### Генерация идёт очень медленно

**Проблема**: Генерация 100+ документов занимает много времени.

**Решения**:
1. Это нормально — каждый запрос занимает 5-15 секунд
2. Уменьшите количество документов: измените множитель `* 5` в списках тем
3. Используйте параллельную обработку (требует модификации кода)

### Документы не сохраняются в Google Drive

**Проблема**: Ошибка при копировании файлов.

**Решения**:
1. Убедитесь, что Google Drive подключен (ячейка должна запросить авторизацию)
2. Проверьте логи ячейки сохранения на наличие ошибок
3. Файлы всегда сохраняются локально в `/content/neurodoc_data/` — их можно скачать вручную

### Проблемы с конвертацией PDF законов

**Проблема**: Ошибки при конвертации PDF в Markdown.

**Решения**:
1. Это нормально — некоторые PDF могут иметь сложную структуру
2. Проверьте логи — будут указаны конкретные файлы с ошибками
3. PDF файлы сохраняются даже если конвертация не удалась — их можно конвертировать вручную

---

## 📊 Результаты

### Статистика генерации

После успешного выполнения проекта вы получите:

- **HR-документы**: 50 файлов в формате Markdown
- **IT-инструкции**: 50 файлов в формате Markdown
- **Онбординг-гайды**: 5 файлов в формате Markdown
- **Compliance документы**: 3 закона РФ в формате Markdown

**Общий объём**: ~100 документов, готовых к использованию в RAG-системах

### Примеры сгенерированных документов

**HR-документ** (`hr_01_политика_удалённой_работы.md`):
```markdown
# Политика удалённой работы

## Введение

Политика удалённой работы определяет...

## Основная часть

### Условия удалённой работы
...
```

**IT-инструкция** (`it_01_инструкция_по_подключению_к_корпоративному_vpn.md`):
```markdown
# Инструкция по подключению к корпоративному VPN

1. Откройте настройки VPN на вашем устройстве
2. Введите следующие параметры:
   - Сервер: vpn.company.com
   - Протокол: OpenVPN
...
```

### Использование результатов

Сгенерированные документы можно использовать для:

1. **RAG-системы**: индексация в векторную базу (Qdrant, Pinecone, Weaviate)
2. **Корпоративные базы знаний**: интеграция в Confluence, Notion, GitBook
3. **Чат-боты**: обучение корпоративных ассистентов
4. **Образовательные платформы**: создание учебных материалов

---

## 📚 Дополнительные ресурсы

### Документация GigaChat API

- [Официальная документация](https://developers.sber.ru/gigachat)
- [Быстрый старт](https://developers.sber.ru/gigachat/quickstart)
- [API Reference](https://developers.sber.ru/gigachat/api/reference)

### Похожие проекты

- [Neuro Video Copywriter](https://github.com/...) — генерация материалов из видео
- [RAG-системы на GigaChat](https://github.com/...) — примеры RAG-реализаций

---

## 📝 Лицензия

Этот проект разработан в образовательных целях и для демонстрации компетенций в области:
- Работы с LLM API (GigaChat)
- Автоматизации генерации контента
- Интеграции с облачными сервисами (Google Colab, Google Drive)
- Обработки документов и конвертации форматов

---

## 🤝 Вклад в проект

Проект находится в активной разработке. Предложения по улучшению приветствуются:

- Добавление новых категорий документов
- Улучшение промптов для лучшего качества
- Оптимизация производительности
- Расширение функциональности (поддержка других LLM, дополнительные форматы)

---

## 📧 Контакты

**Автор**: Дмитрий Блюхеров

Для вопросов и предложений создайте issue в репозитории проекта.

---

<div align="center">


[⬆ Наверх](#neuro-doc-data-generator)

</div>

